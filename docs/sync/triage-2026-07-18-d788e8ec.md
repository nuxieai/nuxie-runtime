# Upstream sync triage: `7c778d13..d788e8ec`

Date: 2026-07-18

Upstream: `rive-app/rive-runtime`

Candidate: `d788e8ec6e8b598526607d6a1e8818e8b637b60c`

Authorization: the cycle-scoped exact-sync authorization recorded in
`docs/upstream-sync-map.md`.

## Inventory and probe

The range contains 60 commits and 416 changed files (`+23,490/-8,884`). The
user's pinned checkout at `7c778d13` remained untouched; all candidate builds
and probes used a clean detached worktree at the candidate SHA.

- The initial default golden probe at the candidate pin had 295 entries: 263
  exact, 584 exact segments, 27 declared diverges, 5 unsupported, and 0
  not-yet. That is retained as the pre-port baseline, not the closeout state.
- The current branch manifest has 317 entries: 287 exact, 27 declared diverges,
  3 not-yet, and 0 unsupported. The three not-yet entries are
  `data_bind_font_test`, `script_advance_test`, and `script_color_data_test`.
- All 27 declared-diverge entries now carry `scripted-status:exact`. The nine
  scripted failures from the initial probe, including
  `script_path_effects_test`, are closed against the candidate oracle. This is
  a strong behavior gate, but it is not by itself proof that luaur implements
  every semantic change in upstream's newer Luau revision.
- The renderer architecture audit is closed for this range. Clip rejection,
  image instance streaming, candidate-generated WGSL through `d788e8ec`, and
  the WebGPU compatibility/no-SSBO fallback are present. The remaining
  renderer commits in the range are backend-only or already represented by
  the Rust architecture; no renderer behavior gap was found.

## Version skew (highest priority)

| surface | upstream change | finding | disposition |
| --- | --- | --- | --- |
| `.riv` format | `b0b126fe` raises support from 7.1 to 7.2 | the 7.2 render path is landed and exact; legacy public `scaleX`/`scaleY` mutation and Image SliceMesh effective-scale state still need direct proof | **LANDED render path; residual behavior proof before pin** |
| Luau | `273da290`, `d1bf9128`, `dd6eb5c0`, plus library-scope work in `ff1a8fa8`/`296742c1` | scoped libraries, Vector, and Mat4 are landed and all scripted corpus gates are green; Rust remains on `luaur-rt =0.1.8` (Luau `8f33df9`) while the candidate uses `rive_0_728_vec3` at `c99c73a96b046723c188dc4f54b02b9d50791325`; upstream's `GPUBuffer:write` overload has no Rust surface | **LANDED subset; WATCH revision conformance and GPUBuffer API** |
| schema/wire | `a2d682e5`, `ff1a8fa8`, `296742c1` | `uint8`, `uint64`, full-width unknown-Uint skipping, and script-library scope are landed | **LANDED** |
| text/layout/bidi/image deps | no HarfBuzz, Yoga, SheenBidi, or decoder revision requiring a Rust dependency bump | behavior changes are in runtime code and fixtures, not a vendored shaping/layout revision | **no dependency action** |

## Initial port candidates

This table records the research-phase priority assignment. The closeout audit
below is authoritative for landed and remaining work.

Rows group commits only when upstream landed one feature as a series.

| upstream sha | title / group | bucket | impact | risk | effort | corpus signal | recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `82440875`, `37fdbe73` | bidirectional-bind source-preference fixes | runtime | critical-fix | high | M | fixtures exist; verify both directions | 10 **PORT** |
| `b0b126fe` | independent image layout-fit scale; format 7.2 | format/runtime | critical-fix | high | M | new upstream fixture | 10 **PORT** |
| `f1c5940c` | validate bindable property instances | runtime | critical-fix | medium | S | no current named diff | 9 **PORT**, add malformed fixture |
| `afc52abf` | advance detached view-model instances at frame end | scripting/runtime | fix | high | M | scripted probe required | 9 **PORT** |
| `bea1f528` | ensure Lua data is initialized | scripting/runtime | fix | high | M | scripted probe required | 9 **PORT** |
| `a2d682e5`, `ff1a8fa8`, `296742c1` | compact Uint types, Uint64, script-library scope, safe unknown Uint skip | schema/wire/scripting | feature + critical-fix | high | L | import fixtures and scripted probe | 10 **PORT** |
| `7a7a3dba` | data-bound font assets | runtime/schema | feature | high | L | corpus coverage required | 8 **PORT** |
| `72b81207` | component lists select the artboard's default state machine | runtime | fix | medium | S | focused structural test | 9 **PORT** |
| `95839f8f` | decode Lua colors as unsigned | scripting/runtime | fix | low | S | scripted fixture | 9 **PORT** |
| `ea1725b7` | nested-artboard origin override | runtime/schema | feature | medium | M | new type/fixture required | 9 **PORT** |
| `833c5122` | clip-bounds rejection and scissor optimization | renderer | perf + fix | high | M | renderer exactness + unit tests | 10 **PORT** |
| `9a9bf409` | image draws move from uniforms to a 64-byte instance stream | renderer | perf | high | L | renderer exactness/perf gates | 10 **PORT** |
| `afd40a99`, `dfaab2f4`, `20070f60` | global view models and follow-up lifecycle fixes | runtime/scripting | feature + fixes | high | L | new fixtures; scripted probe | 8 **PORT** |
| `38d1d3ff` | retain intended scroll index/percent values | runtime | fix | medium | M | add pre-layout/hidden fixture | 9 **PORT** |
| `6007b52c` | script-hosted artboard triggers fire repeatedly | scripting/runtime | critical-fix | high | M | upstream attachment/fixture | 10 **PORT** |
| `49bb3b78` | prevent scripted-data-converter type-change use-after-free | scripting/runtime | critical-fix | high | M | add number/string two-way fixture | 10 **PORT** |
| `43dfc847` | reset and deterministically order text-opacity paint buckets | text/runtime | fix | medium | S | `text_opacity_modifier` exact diff | 10 **PORT** |
| `b6673bb8` | data-binding keyframes | runtime/schema | feature | high | L | upstream fixtures; no current coverage | 8 **PORT** |
| `6e8e8b26` | collapsed advance gates and advance-cycle fixes | runtime/scripting | fix + perf | high | M | likely scripted initialization/lifecycle signal | 10 **PORT** |
| `0f84bc19`, `74ae9ae6`, `dae999ed`, `d7d414dd`, `b1b3714f` | focus ordering, leaf resolution, UI actions, and nested focus-tree fixes | focus/runtime/schema | feature + fixes | high | L | focus corpus coverage incomplete | 8 **PORT** |
| `273da290`, `d1bf9128`, `dd6eb5c0` | Luau 0.726/0.728 and scripting API expansion | scripting/dependency | compatibility + feature | high | L | candidate scripted probe differs | 9 **PORT/WATCH** until luaur compatibility is proven |
| `c214c307` | WebGPU compatibility and low-SSBO mode | renderer | compatibility | high | L | ordinary Dawn unchanged; compatibility adapter not in CI | 8 **PORT** behind an explicit capability gate |
| `6a9cedcc`, `7d4ee62e`, `6359df3a`, `096655e5`, `574bc8b2` | backend scissor/macro/shader-layout/bounds/rename series | renderer | fix + internal | medium | M | direct behavior covered only where architecture overlaps | 6 **PORT matching concepts**, **SKIP backend-only code** |

## Explicit skips / watches

These commits were inspected and have no direct Rust-runtime behavior to port,
or are product surfaces outside this repository. They remain listed so the
range is auditable.

| upstream sha | disposition | reason |
| --- | --- | --- |
| `617d6c5e` | SKIP | recorder service / production thumbnail infrastructure |
| `2e7e064c` | SKIP | editor dependency-panel UI |
| `25971ecc`, `2c5681ff`, `fd18b0b3` | SKIP/WATCH | command-queue volume, semantics, and request-size APIs are host/product APIs, not the current runtime surface |
| `a3e67e62` | SKIP | Apple semantics integration |
| `55a11fa3` | SKIP | native decoder/texture-compression build plumbing; no decoder revision change |
| `2ddd458c` | SKIP | Unreal ORE integration |
| `244aa55b` | WATCH | scripting test helper API; port only if Rust scripting tests consume blob helpers |
| `56c7ef2c` | SKIP | native build-target shader selection |
| `ea0291e7`, `a46ee9af` | SKIP | upstream golden-site/test infrastructure |
| `9fc99964`, `4a2159f6`, `7bffb603` | WATCH | public C++/JS runtime introspection APIs; add Rust API only with a matching consumer |
| `b0d8d6c5`, `66cad9b7`, `0360534d` | SKIP | recorder/player/Android ORE backend fixes outside the pure Rust path |
| `70665773`, `d788e8ec` | SKIP | Unreal/editor-only changes |
| `0eccefad` | SKIP | Android test-app orientation |

## Landed during this cycle

- Renderer: `833c5122` clip rejection/overall bounds, `9a9bf409` 64-byte image
  instance streaming, candidate-generated WGSL, `c214c307` compatibility-mode
  adaptation, and the vertex-storage fallback required by low-SSBO adapters.
- Format and import: the `b0b126fe` 7.2 image layout-fit render path, plus the
  `a2d682e5`/`ff1a8fa8`/`296742c1` compact Uint, Uint64, safe unknown-Uint skip,
  and script-library scope bundle.
- Data binding/runtime: `82440875`/`37fdbe73` directional source preference,
  `f1c5940c` bindable-instance validation, `afd40a99`/`dfaab2f4`/`20070f60`
  global view models, `bea1f528` data initialization, `ea1725b7` nested-origin
  override, `38d1d3ff` scroll intent, and `b6673bb8` per-instance keyframe
  binding.
- Text/state-machine lifecycle: `43dfc847` deterministic text-opacity paint
  buckets, `72b81207` component-list default state-machine selection, and the
  drawable/collapsed lifecycle portions of `6e8e8b26` that are exercised by
  the now-exact corpus.
- Scripting APIs: scoped script libraries and the `dd6eb5c0` Vector and Mat4
  userdata APIs.
- No-code equivalences: Rust's owned `ScriptValue` makes the `49bb3b78`
  converter UAF impossible, and color values are already converted through an
  unsigned `u32` path as required by `95839f8f`.

## Closeout audit: remaining before pin advance

The audit of every behaviorally relevant commit in `7c778d13..d788e8ec`
leaves the following work. Priority reflects the risk of falsely declaring
parity, not implementation size.

| priority | upstream | remaining closure criterion |
| --- | --- | --- |
| P0 | `0f84bc19`, `74ae9ae6`, `d7d414dd`, `dae999ed`, `b1b3714f`, focus portion of `6e8e8b26` | Integrate authored `FocusData` into Artboard/state-machine execution. Materialize hierarchical focus trees; preserve list-virtualization order; resolve a direct parent to its first leaf; implement clear/action/transition/text-input focus behavior; handle nested and swappable artboard scopes; preserve focus across detach/rebuild; add input-driven traces. The current manager and t=0 exact fixtures do not prove this. The golden runner still rejects `FocusData` when input events are present. |
| P0 | `afc52abf`, `6007b52c`, scripted-advance portion of `6e8e8b26` | Complete the root frame-end hook for detached VMIs, exactly-once child traversal, repeated-trigger backing state/reset, and child scripted-artboard `advance(..., false)` behavior. Wire pointer, keyboard, text, and gamepad dispatch to scripted globals and wake script advance; `wake_script_advance_for_global` currently has no production call site and `ScriptMethod` only models pointer methods. Prove with dynamic replay, not the t=0 `reset_shared_viewmodel_instance_test`. |
| P0 | `7a7a3dba` | Finish data-bound font asset resolution and promote `data_bind_font_test` from not-yet with a candidate-exact dynamic trace. |
| P1 | `273da290`, `d1bf9128`, `dd6eb5c0` | Preserve the green 27-entry scripted gate, then either establish differential/conformance evidence for the luaur/Luau revision skew or explicitly hold the pin. Add `GPUBuffer:write(dstOffset, srcOffset, byteLength)` only if/when Rust exposes the corresponding GPUBuffer surface. |
| P1 | `b0b126fe` | Prove legacy pre-7.2 public image-scale state and Image SliceMesh effective render-scale behavior, or document why the Rust API makes the public mutation unobservable while retaining identical output. |
| P1 | `95839f8f` | Promote `script_color_data_test` after its surrounding scripted lifecycle works. The unsigned color conversion itself is already exercised with `0xff102030`; this is an end-to-end proof gap, not a missing cast. |
| P2 | `9fc99964`, `4a2159f6`, `7bffb603` | Keep as API watches: exact C++ wrapper APIs for artboard-name/list-index lookup, enum-name metadata, and named VMI commands are not mirrored, although most underlying data exists. Add only with a matching Rust consumer or an explicit public-API parity requirement. |

Renderer closeout for the highlighted series is complete: `6a9cedcc` overlaps
the landed scissor implementation; `7d4ee62e`, `6359df3a`, and `574bc8b2` are
backend/generated-name changes; and `096655e5` is C++ `LogicalFlush`/Mali
render-target restoration machinery with no equivalent Rust lifetime hazard.

The cycle therefore remains open on the P0 items and the explicit P1 pin
decisions. `LAST_SYNCED_SHA`, active pins, and the clean-manual-cycle count stay
unchanged until those criteria and the default plus scripted ratchets pass at
`d788e8ec`.
