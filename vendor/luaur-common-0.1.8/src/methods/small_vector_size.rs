use crate::records::small_vector::SmallVector;

impl<T, const N: usize> SmallVector<T, N> {
    // Intentionally left empty:
    // SmallVector::size is already implemented on the SmallVector record type.
}
