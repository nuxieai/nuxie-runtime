# Upstream sync triage: `4ac7b327..2bbd8820`

Date: 2026-08-09

Scheduled cycle: 6, approval-gate report

Upstream: `rive-app/rive-runtime`

Candidate cut: `2bbd8820878f38398b6ea6e722cb88310588c8e8`

Nuxie probe tree: `89db3021dde6603d95b26565ddf4ac4ae731f410`

Authorization: inventory, classification, probes, and issue filing only.
Standing approvals are `none`; this cycle made no product port, manifest edit,
LAST_SYNCED_SHA change, or pin movement.

## Run qualification

The candidate worktree resolved exactly to `2bbd8820878f38398b6ea6e722cb88310588c8e8`.
The pinned checkout stayed at `4ac7b32798da0482e441ef09304dc3b480ed3ee5`.
The prior S5 report covers `4ac7b327..74c0d601` and records an explicit HOLD,
so this is a new S6 cycle rather than an S5 top-up. S5 row IDs are preserved
and their open recommendations are carried below.

Disk qualification passed with 50 GiB free on `/System/Volumes/Data`; no idle
worktree target directory was removed.

## Inventory

The full open range contains **18 commits, 295 changed files, 22,671
insertions, and 2,406 deletions**. S5 classified the first eight commits. The
S6 addition is ten commits (`74c0d601..2bbd8820`), 136 changed files, 5,269
insertions, and 1,648 deletions.

| row | upstream sha | title | bucket | impact | risk | effort | corpus signal | recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| S6-1 | `0a8499b8` | Refactor the GM testing framework | renderer/test infrastructure | internal | low | S | none; no product behavior is reachable | 2 **SKIP** — upstream test/player plumbing, with no Nuxie runtime subset |
| S6-2 | `f41cd8f3` | Reset legacy layout transforms | format + runtime/layout + fixtures | fix | high | M | none — coverage gap: enrolled corpora lack a pre-7.3 layout carrying stale transforms | 10 **PORT** — preserve pre-7.3 behavior and import the compatibility fixtures as one format/runtime slice |
| S6-3 | `309e901f` | Expose global view models to Lua | scripting/runtime + renderer query | feature/fix | high | M | none — coverage gap: the new global-view-model fixture is not enrolled | 8 **PORT** the scripting/global-VM runtime subset; keep the flag-gated deferred path-query renderer subset under WATCH |
| S6-4 | `3fc5a336` | Store rarely-set properties in lazy sidecars | generated C++ storage + schema tooling | internal/performance | low | S | none; wire keys and behavior are unchanged | 2 **SKIP** — C++ object-layout optimization; confirm future schema generation tolerates the metadata |
| S6-5 | `7502fd3f` | Add image sampler filter and wrap options | schema + runtime/image + renderer API | feature | high | M | none — coverage gap: upstream adds focused C++ tests but no enrolled authored asset | 8 **PORT** — add schema keys, inherited/per-node options, render API mapping, and a focused fixture together |
| S6-6 | `3ed35ee0` | Begin image-backed RenderPaint | renderer | feature | high | L | none — disabled GM and no corpus asset; upstream explicitly leaves atomic and other renderer work incomplete | 5 **WATCH** — define the wgpu image-paint mapping and add focused pixel evidence before porting |
| S6-7 | `2cfa84e8` | Retry Lua module registration and make scripted disposal safe | scripting/runtime | fix | high | M | none — coverage gaps: no cyclic/missing-module retry or teardown/backpointer fixture | 9 **PORT** — port the registration skip/retry rules and disposal safety with focused tests |
| S6-8 | `11217a52` | Share the deferred host layer and inline player | renderer/runtime host | internal/feature | high | L | none new; existing deferred-session signal remains attributed to S5-6 | 6 **WATCH** — fold the shared-host adaptation into the S5-6 wgpu session map, excluding native host/player machinery |
| S6-9 | `2bbd8820` | Add 7.3 layout offsets, anchors, constraints, and virtualized scroll buffering; bump Luau 0.733 | format + schema + runtime/layout + scripting dependency | feature/fix | high | L | none — coverage gap: new layout/scroll fixtures are not enrolled; scripted candidate builds successfully with 0.733 | 10 **PORT** runtime/schema/layout/scroll; 8 **WATCH** the Luau engine delta until its semantic rung is reviewed |

Two commits are intentionally outside the actionable row table:

- `1de56230` changes only upstream CI/coverage scripts. Score 1 **SKIP**.
- S5 build-only `8efe18ec` remains score 1 **SKIP**.

The other seven S5 rows remain classified exactly as in
`docs/sync/triage-2026-08-05-74c0d601.md`: S5-1 SKIP; S5-2, S5-3,
S5-4, S5-5, and S5-7 PORT but deferred; S5-6 WATCH. Thus every commit in the
18-commit open span has a classification.

## Port-manifest inventory

The required command ran against the exact candidate without regenerating the
manifest:

```text
RIVE_RUNTIME_DIR=/Users/levi/dev/worktrees/rive-candidate-s5-2bbd8820 make port-manifest-check
```

It exited 2 at the expected provenance guard because the checked-in manifest
still names `4ac7b327` and the candidate is `2bbd8820`. An independent sorted
inventory found 457 candidate non-generated `src/**/*.cpp` paths and 456
manifest rows.

- Missing: `src/text/text_style_background.cpp`, introduced by S5-2.
- Stale: none.
- New S6 path drift: none.

## Commit evidence and boundaries

### S6-2 — legacy layout compatibility

The upstream file minor version moves from 7.2 to 7.3. Before evaluating
transformable layout behavior, files authored before 7.3 have stale layout
rotation/scale reset so previously ignored values do not suddenly collapse or
move content. This is an import-compatibility gate, not a cosmetic layout
change, and its old/new fixtures must land with the port.

### S6-3 — globals in scripts

Lua scripts can resolve global view models from the runtime instance. The same
commit also mirrors deferred path queries into the renderer behind an upstream
feature flag. Nuxie's global-VM scripting surface is active and should port;
the renderer query is separable and remains WATCH pending the deferred-session
adaptation.

### S6-5 and S6-6 — image sampling and image paint

S6-5 adds authored sampling filter/wrap choices on image assets with node-level
overrides and passes them into rendering, an active schema/runtime/API surface.
S6-6 starts image-backed paints but is explicitly incomplete upstream and its
GM is disabled. Treating the latter as WATCH avoids copying a partial native
renderer design before a wgpu contract and pixel oracle exist.

### S6-7 — scripting lifetime correctness

Module registration is sorted and retried while skipping unavailable modules,
and scripted-object disposal clears backpointers safely. Both affect active
Nuxie scripting ownership. Candidate C++/Luau 0.733 builds and executes this
surface, but the current corpus lacks the failure and teardown shapes.

### S6-8 and S6-9 — host, layout, and scroll

S6-8 moves deferred host/frame-sink behavior into shared runtime code and adds
an inline player. It extends the architecture already isolated in S5-6, so the
portable wgpu subset should be designed once rather than ported commit by
commit. S6-9 changes the product format and active runtime semantics: stored
x/y become offsets from solved layout positions, component origins become
anchors, every positioning constraint honors them, and virtualized scroll
buffering becomes authored state. These changes are gated on `.riv` 7.3.

## Version skew

| surface | finding | disposition |
| --- | --- | --- |
| Luau bytecode/vendor | candidate moves `rive_0_732` to `rive_0_733`; candidate dependency HEAD is `f4a8c732`, and both sides still report bytecode MIN=3, MAX=13, TARGET=7 | bytecode numbers are compatible and the scripted C++ candidate builds; WATCH the engine semantic delta until the normal fork/rung review is complete |
| `.riv` header/format | `File::minorVersion` moves 7.2 to 7.3 in S6-2, and S6-9 gates new layout/scroll semantics on it | pin advance is policy-coupled to S6-2 and S6-9; import old and new fixtures before moving any product pin |
| shaping/layout/bidi | no Yoga, HarfBuzz, or SheenBidi revision changes; runtime layout semantics change directly | port runtime semantics and fixtures; no third-party dependency port inferred |
| image codecs | image sampling and paints change, but codec revisions do not | no codec action; test sampling/render behavior with authored assets |
| renderer/build interfaces | S6-6/S6-8 add native renderer and host interfaces; candidate C++ oracle compiles without a product interface patch | adapt only the wgpu-owned subset after focused evidence |

## Probe results and attribution

The ordinary and scripted golden probes used the candidate through a
throwaway edit to `tools/golden-runner/runtime-provenance.sh`. That file was
restored before commit; the product pin and manifest never changed.

Ordinary, excluding the inherited `db_health_tracker` Rust hang:

```text
entries=363 exact=348 exact-segments=1132 side-channel=1127 diverges=10 not-yet=5
```

Scripted, excluding inherited Rust hangs and with the final runner-invalidated
batch rebuilt and rerun:

```text
entries=362 exact=346 exact-segments=1126 side-channel=1121 diverges=11 not-yet=5
final rerun: entries=31 exact=26 exact-segments=38 side-channel=38 diverges=0 not-yet=5
```

In both modes, the only behavioral failures were `jellyfish_test` at line 136
and `tape` at line 21: the known `makeRenderPaint` versus `makeRenderBuffer`
import-order signature. S5's oracle bisect already attributes both exactly to
S5-6 (`e949498e`). No S6 commit creates a new corpus diff, so attribution is
complete for all observed differences. Every no-signal PORT row above names a
coverage gap.

Pin-advance couplings:

- **Mechanical corpus coupling:** S5-6 must disposition the two import-order
  differences before the golden ratchet can accept any candidate at or after
  `e949498e`.
- **Format policy coupling:** S6-2 and S6-9 must preserve 7.2 imports while
  adding 7.3 semantics before the product pin advances.
- **Scripting dependency coupling:** the candidate scripted oracle consumes
  Luau 0.733. Bytecode version numbers match, but its semantic rung remains an
  explicit S6-9 WATCH slice.

### Inherited clean-main probe breakage

These failures reproduce with Nuxie `origin/main`; only the C++ oracle points
at the candidate, so they are not attributable to the upstream span:

1. Ordinary Rust `db_health_tracker` does not return within 90 seconds after
   the candidate C++ side finishes.
2. Scripted Rust `data_viz_demo` does not return within 90 seconds after the
   candidate C++ side finishes.
3. Scripted Rust provenance invalidation can remove the C++ scripted runner,
   producing runner-not-found failures until that runner is rebuilt; one later
   batch saw the runner disappear again and passed after a targeted rebuild.

The candidate oracle itself builds and runs. The two hangs are not a small,
obvious stale literal or invocation fix, so this triage cycle does not change
product or harness code speculatively. They are landing-battery blockers if
the corresponding gates exercise those entries.

## Deferred re-triage

The prior S5 HOLD increments each carried counter once. Existing Universe
issues are retained and are not re-filed.

| deferred id / rows | prior | S6 | current evidence | recommendation |
| --- | ---: | ---: | --- | --- |
| S5-2, S5-3, S5-4, S5-5, S5-7 (`deferred-2026-08-05`) | 1 | 2 | no new corpus signal; their named coverage gaps remain | retain PORT recommendations pending approval |
| S5-6 (`deferred-2026-08-05`) | 1 | 2 | still owns both attributed golden diffs; S6-8 extends its host architecture | retain 6 WATCH; refresh one deferred-session/wgpu adaptation map |
| `deferred-2026-07-19-ore-gpu` | 5 | 6 | S6-8 reinforces shared deferred hosting but not a portable native ORE design | retain 6 WATCH under S5-6/S6-8 |
| `deferred-2026-08-02-wgpu-clip-planes` | 2 | 3 | no focused wgpu counter or pixel evidence | retain 5 WATCH |
| `deferred-2026-08-02-triangulation-cache` | 2 | 3 | no renderer measurement or pixel evidence | retain 5 WATCH |
| S4-45 command-queue blob (`deferred-2026-08-05`) | 2 | 3 | the typed Rust queue remains active and the four ledger cases remain the exact boundary | retain 8 PORT pending approval |

The prior Luau engine deferral stays closed. S6-9's 0.733 bump is a new
ordinary WATCH slice, not a revival of that stale deferral.

## Proposed approval order

| priority | row | score | action | ordering note |
| ---: | --- | ---: | --- | --- |
| 1 | S6-2 | 10 | PORT | establish 7.3 import compatibility first |
| 2 | S6-9 | 10 / 8 | PORT runtime; WATCH Luau | layout/scroll semantics depend on the 7.3 gate |
| 3 | S6-7 | 9 | PORT | scripting correctness with focused retry/disposal tests |
| 4 | S6-3 | 8 | PORT runtime subset | globals are active; path-query renderer subset stays WATCH |
| 5 | S6-5 | 8 | PORT | schema/runtime/render API plus an authored image fixture |
| 6 | S6-8 | 6 | WATCH | combine with the S5-6 wgpu adaptation map |
| 7 | S6-6 | 5 | WATCH | wait for a complete upstream contract and wgpu pixels |
| 8 | S6-1, S6-4 | 2 | SKIP | no portable product behavior |

## Universe issue ledger

Existing rows were deduplicated before creation: S5-2 `UNIV-1675`, S5-3
`UNIV-1673`, S5-4 `UNIV-1676`, S5-5 `UNIV-1672`, S5-6 `UNIV-1678`, S5-7
`UNIV-1674`, and S4-45 `UNIV-1677`.

The first create was verified with `universe issue view`: project, priority,
and description all landed before the remaining issues were batched.

| row | issue |
| --- | --- |
| S6-2 | `UNIV-1874` |
| S6-3 | `UNIV-1875` |
| S6-5 | `UNIV-1876` |
| S6-6 | `UNIV-1877` |
| S6-7 | `UNIV-1878` |
| S6-8 | `UNIV-1879` |
| S6-9 | `UNIV-1880` |

## Approval gate

Attribution is complete for all observed corpus differences, but coverage gaps
remain explicit and clean-main probe failures may block the landing battery.
Standing approvals remain `none`.

**No ports or pin moves were made. Review this report and approve specific
rows before scheduling any implementation.**
