# Upstream sync triage: `d788e8ec..4ac7b327`

Date: 2026-08-02

Manual cycle: 4, S4C final probe (approval-ready)

Upstream: `rive-app/rive-runtime`

Nuxie probe tree: `ea8f67223c405910fa7ae1f8bb818e62876edd13`

Fetched candidate cut: `4ac7b32798da0482e441ef09304dc3b480ed3ee5`
(`origin/main` in the reference repository and clean detached candidate HEAD)

Authorization: inventory and triage only. Standing approvals are `none`. No
upstream row in this report is approved for porting, no product pin moved, and
no probe branch was landed.

## Run qualification

The fetched-cut qualification is now complete. The reference repository's
`origin/main` and the clean candidate at
`/Users/levi/dev/worktrees/rive-candidate-s4` both resolve to `4ac7b327`; the
reference checkout itself remains detached at `d788e8ec`.

The behavioral qualification is complete. The orchestrator supplied the exact
offline dependencies under `.s4b-work/dependencies`:

```text
Yoga rive_changes_v2_0_1_2_grid: bd261943260d957df73189a3d6f4dab4cd55947c
Luau rive_0_732:                 86eb00965bebd6d31f3c97c86e1a94918172ec5a
Luau rive_0_730 control:         835af7b50381840a983853317d181a533b75585b
```

Both candidate ratchets ran at clean detached
`4ac7b32798da0482e441ef09304dc3b480ed3ee5` with
`RIVE_RUNTIME_DIR=/Users/levi/dev/worktrees/rive-candidate-s4`. The ordinary
ratchet completed all 321 entries/657 segments with 100 attributed failures;
the scripted ratchet completed the same corpus with 104 attributed failures.

S4-38 makes `layout_sizing_style.hpp` include Yoga publicly. The throwaway
golden-runner Premake file therefore needed the supplied dependency roots and a
forced `rive_yoga_renames.h` include; the scripted runner also needed the exact
0.732 Luau root and `rive_luau.hpp`. These are probe-only build-interface
adaptations in `.s4b-work/nuxie-probe`, not product changes. The scripted target
uses `RIVE_GOLDEN_SCRIPTING_OUT` (not `RIVE_GOLDEN_RUNTIME_OUT`). No older
dependency was substituted in either final candidate run; 0.730 and pre-grid
Yoga were used only for parent controls.

## Inventory

The full `LAST_SYNCED_SHA..origin/main` range contains **48 commits, 345
changed files, 14,518 insertions, and 2,865 deletions**. The S4B top-up adds
**25 commits, 240 changed files, 10,788 insertions, and 2,080 deletions** beyond
the predecessor's `e0d4913f` cut.

| id | upstream sha | title | bucket | impact | risk | effort | corpus signal | recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| S4-1 | `1b4df2ad` | TextInput improvements | runtime + fixture | fix | high | M | **yes:** ordinary `text_input`; prior S3 scripted probe also saw it | 8 **PORT** — carry-forward S3-1, staleness 1 |
| S4-2 | `079305d7` | Performance tab updates | runtime/profiler | perf | low | S | **no new upstream signal:** profiler-only | 6 **PORT (satisfied)** — prior S3-2 WATCH landed independently as D16; no S4 action |
| S4-3 | `b73bc675` | Statically link library `require`s | schema + runtime + scripting + fixtures | critical-fix | high | L | **yes scripted:** `scope_probe`; C++ completes while Rust SIGTRAPs on a luaur stack assertion | 9 **PORT** — carry-forward S3-3, staleness 1 |
| S4-4 | `ba2b6434` | Add unset methods for global view models | runtime + fixture | fix | high | M | **no ordinary signal:** relevant scripted fixtures are skipped and new fixture is unenrolled | 8 **PORT** — add the upstream global-view-model fixture |
| S4-5 | `073942e9` | Tag ORE GM shader modules with fixture asset IDs | renderer fixture | internal | low | S | **no:** ORE GM is outside the runtime corpus | 4 **WATCH** — retain for an ORE renderer corpus |
| S4-6 | `7fb8eecc` | Better Unreal GM support | build/test | internal | low | S | **no:** Unreal deploy harness only | 1 **SKIP** |
| S4-7 | `beb246e5` | Focus dirty flag avoids per-frame tree walks | runtime | perf | high | M | **no:** focus corpus remains behaviorally exact at the pre-`e0` control | 7 **PORT** — dirt propagation is active flow-runtime work |
| S4-8 | `18411981` | Tree-shake unused scripts from runtime export | schema + scripting/editor export | feature | high | M | **no enrolled signal:** final scripted corpus completes; export-only coverage remains absent | 7 **PORT** — regenerate schema and add export/import coverage |
| S4-9 | `fa531112` | Advertise `colorBufferHalfFloat` on Metal | renderer | fix | low | S | **no runtime-corpus signal:** renderer lane not changed by the probe | 2 **SKIP** — Metal-backend capability is not a Nuxie/wgpu implementation change |
| S4-10 | `e85a1160` | Bidirectional binding for stateful component properties | runtime + fixture | feature | high | M | **no ordinary signal:** new upstream fixture is unenrolled | 8 **PORT** — data binding is an active product surface |
| S4-11 | `30a0e2d4` | Guard null databound nested artboards | runtime + fixture | critical-fix | high | M | **no ordinary signal:** crash fixture is unenrolled | 10 **PORT** — deterministic first-advance crash |
| S4-12 | `7d3d1fc0` | Unreal ORE GM support step 1 | renderer fixture/build | internal | low | S | **no:** ORE/Unreal GM harness only | 2 **SKIP** |
| S4-13 | `fd64cbb5` | Copy thread data to async scripting coroutines | scripting + fixture | critical-fix | high | M | **no enrolled signal:** final scripted corpus completes; crash fixture is unenrolled | 10 **PORT** — fixes a null-pointer crash in `async(...)` host APIs |
| S4-14 | `e62b2bf3` | Guard scripted converter before hydration | runtime + scripting | critical-fix | high | S | **no isolated final signal:** scripted corpus completes | 9 **PORT** — localized correctness guard |
| S4-15 | `0c62fced` | Rev Luau to `rive_0_730` | scripting vendor dependency | fix | high | L | **superseded:** final 0.732 candidate runner completed the full C++ corpus | 8 **WATCH** — no standalone port; resolve the final Luau/luaur matrix |
| S4-16 | `482b24a1` | Virtualized scroll sizing/overscroll and listener-event fixes | runtime + fixtures | fix | high | M | **no ordinary signal:** existing ordinary corpus is green before `e0`; new fixtures are unenrolled | 8 **PORT** — add both fixtures |
| S4-17 | `8af27351` | Nested view-model properties mint referenced types from scripts | scripting + fixture | fix | high | M | **no enrolled signal:** final scripted corpus completes; new fixture is unenrolled | 8 **PORT** |
| S4-18 | `0a2e478a` | Propagate opacity/layout to paused nested artboards | runtime + fixtures | fix | high | M | **no pre-`e0` signal:** new paused fixture is unenrolled | 8 **PORT** |
| S4-19 | `15da0652` | Return computed width/height for images | runtime + fixture | fix | medium | M | **no ordinary signal:** new image fixture is unenrolled | 8 **PORT** |
| S4-20 | `e45ca938` | Incredibuild support | build | internal | low | S | **no:** build tooling only | 1 **SKIP** |
| S4-21 | `f5cfee3a` | Size text to parent layout with min/max applied | runtime + fixtures | fix | high | M | **yes ordinary + scripted:** `data_binding_test`; scripted also adds `data_viz_demo`; parent `e45ca938` is green | 8 **PORT** |
| S4-22 | `b1d97fe7` | Preserve host-bound view models in `riveLuaPushArtboard` | scripting + runtime | critical-fix | high | M | **no added scripted diff:** targeted parent/child controls preserve the S4-21 failures | 9 **PORT** |
| S4-23 | `e0d4913f` | Add opacity and transform support on artboards | runtime + fixtures | feature | high | L | **yes:** 96 ordinary and 99 scripted entries; parent control isolates the wave | 8 **PORT** — broad renderer-command and nested-artboard surface |
| S4-24 | `fc995413` | Font view-model data binding from Luau | scripting + runtime | feature | high | M | **yes ordinary:** `data_bind_font_test`; `e0d4913f` is green and the child introduces `restore`/`save` divergence | 8 **PORT** — mirror `getFont`/`pushFont` ownership and wasm export coverage |
| S4-25 | `40ff578d` | Rev Luau to `rive_0_731` | scripting vendor dependency | fix | high | L | **superseded:** S4-43 advances the final cut to tested 0.732 | 8 **WATCH** — no standalone port; validate the final pin |
| S4-26 | `1f447b91` | Harden file import against truncated/corrupt input | runtime + tests | critical-fix | high | S | **unenrolled:** new malformed-input tests are outside the golden corpus | 10 **PORT** — prevents invalid reads and partial-import destruction hazards |
| S4-27 | `6fcceeb4` | Compile-time Luau atom lookup and Unreal/Linux build refactor | scripting + build | perf | medium | M | **no added scripted diff:** full lane completes; Unreal portion is outside product scope | 6 **PORT** the atom table; **SKIP** Unreal packaging |
| S4-28 | `b6b0b334` | Combine Vulkan MSAA fast-path fill subpasses | renderer/Vulkan | perf | low | L | **no:** Vulkan pixel path is outside ordinary commands and Nuxie uses wgpu | 3 **SKIP** — backend-specific implementation |
| S4-29 | `38c92412` | Skip non-solo-set children for solo data binding | runtime + fixtures | fix | high | M | **unenrolled:** new solo silver is not in the Nuxie corpus; final probes add no isolated signal | 8 **PORT** — active data-binding correctness |
| S4-30 | `e06c3583` | Remove inner feather when converting fill to stroke | runtime/renderer + silver | fix | medium | S | **yes ordinary + scripted:** `echo_show_demo`; `38c92412` is green and the child introduces `clipPath`/`drawPath` divergence | 8 **PORT** — normalize invalid stroke feather state |
| S4-31 | `c610850c` | Unreal gold refactor plus wgpu MSAA clip planes | renderer + build/test | perf | medium | M | **no:** ordinary lane does not render pixels; Unreal harness is out of scope | 5 **WATCH** — retain the wgpu clip-plane optimization for renderer parity |
| S4-32 | `742e01b1` | Hook GL blend/MSAA extensions through Wagyu | renderer/WebGL compatibility | feature | low | M | **no:** Wagyu/WebGL adapter is not Nuxie's native wgpu path | 3 **SKIP** |
| S4-33 | `a489c461` | Test rive_native Windows fence-thread teardown fix | rive_native test | critical-fix | low | S | **no:** only the upstream rive_native harness changed | 1 **SKIP** — no Nuxie command/runtime implementation delta |
| S4-34 | `1b79fb9f` | More Incredibuild/Ninja support | build | internal | low | S | **no:** Windows build tooling only | 1 **SKIP** |
| S4-35 | `353ef4fc` | Propagate image/font into nested exposed asset properties | runtime + fixtures | critical-fix | high | M | **unenrolled:** new stateful-image fixture is outside the corpus; final probes add no isolated signal | 9 **PORT** — fixes nested asset identity/ownership propagation |
| S4-36 | `4a10679b` | Make interior triangulations fully cacheable | renderer | perf | medium | M | **no:** ordinary commands cannot observe GPU triangulation caching | 5 **WATCH** — adapt only with renderer counter/golden evidence |
| S4-37 | `5da2a6dc` | Fix Unreal RHI Vulkan atomics | renderer/Unreal | fix | low | S | **no:** Unreal-only shader/deploy surface | 1 **SKIP** |
| S4-38 | `3de78b0d` | Layout participants, grid, stack, transforms, and transparent containers | schema + runtime + Yoga + fixtures | feature | high | L | **build-interface signal, no enrolled diff:** exact Yoga grid builds; public Yoga include/rename requires harness adaptation; 18 fixtures remain unenrolled | 10 **PORT** — foundational format/layout wave; land only with Yoga and exact fixtures |
| S4-39 | `fd08096e` | Prevent UAF while remapping text-run listeners | runtime | critical-fix | high | S | **no dedicated enrolled fixture; both final probes complete** | 10 **PORT** — memory-safety fix |
| S4-40 | `f19497e6` | Unreal RHI MSAA shader update | renderer/Unreal | fix | low | S | **no:** Unreal-only shader path | 1 **SKIP** |
| S4-41 | `caa91f21` | Build layout backgrounds without a Rectangle | runtime/layout + tests | perf | medium | M | **no added enrolled diff:** both final probes complete | 6 **PORT** — Nuxie already owns direct retained layout paths; adapt the scratch-path/empty-work optimization |
| S4-42 | `36aabf60` | Blob view-model data binding with script access | schema + runtime + scripting + fixtures | feature | high | L | **no enrolled signal:** new blob fixture is unenrolled; full scripted lane completes | 10 **PORT** — add type keys 1043/1044, retained blob values, binding, and script access together |
| S4-43 | `395defdb` | Suppress dithering at alpha 0; rev Luau to `rive_0_732` | renderer shaders + scripting vendor | fix | high | M/L | **no command diff:** exact 0.732 C++ runner completes 321 entries; renderer pixels remain a separate lane | 8 **PORT/WATCH** — port the alpha-zero shader guard; hold the Luau pin pending compatibility proof |
| S4-44 | `3b6690dd` | Run Unreal goldens on Windows UE 5.7 | CI/test | internal | low | S | **no:** Unreal CI only | 1 **SKIP** |
| S4-45 | `3c77a64d` | Command-queue AssetBlob support | command queue/runtime + tests | feature | low | M | **no:** command queue/server are explicitly absent as Nuxie feature F3 | 4 **WATCH** — preserve for a future F3 opening; do not port now |
| S4-46 | `f7c84546` | Stable identity for scripted asset `Property.value` reads | scripting + tests | critical-fix | high | S | **no isolated scripted diff:** full lane completes; new identity tests are unenrolled | 9 **PORT** — fixes asset wrapper identity/lifetime semantics |
| S4-47 | `4738c7ae` | FileFormat protocol and runtime blob/layout-fit support | schema + scripting/editor + runtime | feature | high | L | **no enrolled signal:** both ratchets complete; FileFormat editor hosts remain outside corpus | 8 **PORT** the runtime/schema subset; **SKIP** editor LSP/preview hosting |
| S4-48 | `4ac7b327` | Single non-root updatePass for layout-fit leaf reflow | runtime/layout | fix | high | S | **no added enrolled diff:** both final probes complete | 9 **PORT** — same-frame nested layout correctness and less duplicate work |

### Bucket coverage

Every commit is represented. The top-up contains active schema/runtime/scripting
work, explicit backend/harness SKIPs, and mixed commits whose product-relevant
hunks are called out separately. The largest new active wave is S4-38; S4-41,
S4-47, and S4-48 depend on or refine that layout surface. S4-42 owns blob
view-model support; S4-45 remains WATCH because the command queue/server are an
explicit Nuxie feature gap rather than a partially ported surface.

## Port-manifest inventory

The required command was run at exact candidate HEAD:

```text
RIVE_RUNTIME_DIR=/Users/levi/dev/worktrees/rive-candidate-s4 make port-manifest-check
```

The checker found **10 missing rows**:

`src/animation/keyframe_int.cpp`, `src/component_origin.cpp`,
`src/core/field_types/core_int_type.cpp`,
`src/data_bind/context/context_value_asset_blob.cpp`,
`src/layout/grid_item_placement.cpp`, `src/layout/grid_track.cpp`,
`src/layout/layout_participant.cpp`, `src/layout/layout_sizing_style.cpp`,
`src/viewmodel/runtime/viewmodel_instance_asset_blob_runtime.cpp`, and
`src/viewmodel/viewmodel_instance_asset_blob.cpp`.

An independent sorted-set comparison found **456 candidate non-generated
`src/**/*.cpp` paths and 447 manifest rows**, confirming those 10 missing rows
and one stale row: `src/nested_artboard_origin.cpp`. S4-38 accounts for seven
missing rows and the stale row; S4-42 accounts for the remaining three missing
rows. The manifest was not regenerated, edited, or reclassified.

Pin consistency remains intentionally split at the approval boundary:

- product runtime pins and `LAST_SYNCED_SHA`: `d788e8ec`;
- manifest inventory pin and `port-manifest.toml`: `b73bc675`;
- fetched S4 candidate: `4ac7b327`.

The predecessor's inventory-registry finding also remains open:
`tools/golden-runner/runtime-provenance.sh` hard-codes the product runtime SHA
but is absent from the map's current-revision pin registry. The throwaway probe
had to bump it before accepting the candidate. Add it only in a separately
approved map-maintenance change.

## Candidate probes

The throwaway probe clone is
`/Users/levi/dev/worktrees/nuxie-p1a-binary/.s4b-work/nuxie-probe`. It bumps the
four registered current-revision pins plus the unregistered golden provenance
pin only inside that clone. The S4 branch retains all product pins.

### Ordinary comparison

The final candidate comparison completed **321 exact entries and 657 exact
segments**, with **100 failing entries**, all attributed. The predecessor's 98
failures are present with the same first-mismatch signatures and retain their
original attribution; only `data_bind_font_test` and `echo_show_demo` are new.

| attribution | entries | proof |
| --- | ---: | --- |
| `1b4df2ad` S4-1 | 1 | `text_input`; the same failure exists at `b1d97fe7`, matching the prior S3 probe |
| `f5cfee3a` S4-21 | 1 | `data_binding_test`; `e45ca938` parent control is green and the child moves the text/layout transform by 5px |
| `e0d4913f` S4-23 | 96 | all other failures disappear at the `b1d97fe7` parent control; the first mismatch is the newly emitted artboard transform/opacity command in each stream |
| `fc995413` S4-24 | 1 | `data_bind_font_test`; green at `e0d4913f`, red at the child with Rust `restore` versus C++ `save` |
| `e06c3583` S4-30 | 1 | `echo_show_demo`; green at `38c92412`, red at the child with Rust `clipPath` versus C++ `drawPath` |

The 96 S4-23 entries remain:

`animation_reset_cases`, `artboardclipping`, `bad_skin`, `ball_test`,
`bidirectional_binding_target_1`, `bidirectional_binding_target_2`,
`blend_test`, `bullet_man`, `circle_clips`, `click_event`, `clip_tests`,
`complex_ik_dependency`, `cubic_value_test`, `custom_image_name`,
`db_health_tracker`, `dependency_test`, `distance_constraint`,
`double_library_with_image`, `double_line`, `draw_rule_cycle`, `ellipsis`,
`entry`, `event_on_listener`, `events_on_states`, `fix_rectangle`,
`follow_path`, `follow_path_path`, `follow_path_path_0_opacity`,
`follow_path_shapes`, `follow_path_solos`, `follow_path_with_0_opacity`,
`hello_world`, `hosted_font_file`, `hosted_image_file`, `hunter_x_demo`,
`in_band_asset`, `jellyfish_test`, `joel_signed`, `joystick_flag_test`,
`juice`, `library_export_animation_test`, `library_export_state_machine_test`,
`library_export_test`, `library_scope_edge_test`, `library_with_image`,
`light_switch`, `lock_icon_demo`, `long_name`, `looping_timeline_events`,
`magic_alley_db_reduced_export`, `modifier_test`, `modifier_to_run`,
`multiple_state_machines`, `nested_artboard_opacity`, `nested_event_test`,
`nested_library_scope_test`, `nested_solo`, `new_text`, `off_road_car`,
`oneshotblend`, `opaque_hit_test`, `parent_child_opacity_univ_1278`,
`pointer_events`, `pointer_events_nested_artboards_in_solos`, `quantize_test`,
`rewards_demo`, `rocket`, `rotation_constraint`, `runtime_nested_inputs`,
`runtime_nested_text_runs`, `scale_constraint`, `scroll_test`, `shapetest`,
`solo_test`, `solos_collapse_tests`, `solos_with_nested_artboards`, `sound`,
`sound2`, `spotify_kids_app_icon`, `state_machine_transition`,
`stroke_name_test`, `tape`, `test_elastic`, `test_modifier_run`,
`text_style_feature_fl_e8`, `text_variation_modifier_fl_e8`,
`timeline_event_test`, `transform_constraint`, `transition_duration_bind_list`,
`transition_duration_bind_nested`, `translation_constraint`, `trim`,
`trim_path_linear`, `two_artboards`, `two_bone_ik`, and `walle`.

The completed final-cut command was:

```text
DEPENDENCIES=/Users/levi/dev/worktrees/nuxie-p1a-binary/.s4b-work/dependencies RIVE_GOLDEN_YOGA_DIR=/Users/levi/dev/worktrees/nuxie-p1a-binary/.s4b-work/dependencies/rive-app_yoga_rive_changes_v2_0_1_2_grid RIVE_GOLDEN_RUNTIME_OUT=/Users/levi/dev/worktrees/nuxie-p1a-binary/.s4b-work/runtime-out/ordinary-debug RIVE_RUNTIME_DIR=/Users/levi/dev/worktrees/rive-candidate-s4 make golden-compare
```

The target exits nonzero because exact mismatches are findings, not because the
runner failed. Candidate provenance, all C++ streams, and all Rust comparisons
completed.

### Scripted comparison

The first full scripted final-cut comparison completed **321 exact entries and
657 exact segments**, with **104 failing entries**, all attributed:

| attribution | entries | fixtures and boundary proof |
| --- | ---: | --- |
| `1b4df2ad` S4-1 | 1 | `text_input`; retained from the prior S3 scripted probe and present at all later controls |
| `b73bc675` S4-3 | 1 | `scope_probe`; C++ stream completes, Rust exits SIGTRAP on `lua_pushcclosurek`'s stack assertion |
| `f5cfee3a` S4-21 | 2 | `data_binding_test`, `data_viz_demo`; both are green at parent `e45ca938` and fail at S4-21 |
| `e0d4913f` S4-23 | 99 | the 96 ordinary S4-23 fixtures above plus `computed_values_test`, `scripted_interpolator`, and `scripted_memory_leak`; the three additions are green at parent `b1d97fe7` |
| `e06c3583` S4-30 | 1 | `echo_show_demo`; the full `e0d4913f` scripted control has 103 failures and the final cut adds only this row |

`data_bind_font_test`, the S4-24 ordinary addition, is green in the scripted
comparison. The full `e0d4913f` scripted control used pre-grid Yoga and exact
Luau 0.730 and produced 103 failures. Targeted S4-21/S4-23 boundary controls
then separated `data_viz_demo` from the three S4-23-only additions; no failure
is assigned merely by title or file overlap.

The completed final command was:

```text
DEPENDENCIES=/Users/levi/dev/worktrees/nuxie-p1a-binary/.s4b-work/dependencies RIVE_GOLDEN_YOGA_DIR=/Users/levi/dev/worktrees/nuxie-p1a-binary/.s4b-work/dependencies/rive-app_yoga_rive_changes_v2_0_1_2_grid RIVE_GOLDEN_LUAU_DIR=/Users/levi/dev/worktrees/nuxie-p1a-binary/.s4b-work/dependencies/luigi-rosso_luau_rive_0_732 RIVE_GOLDEN_SCRIPTING_OUT=/Users/levi/dev/worktrees/nuxie-p1a-binary/.s4b-work/runtime-out/scripted-release RIVE_GOLDEN_DECODERS_OUT=/Users/levi/dev/worktrees/nuxie-p1a-binary/.s4b-work/decoders-out/scripted-release RIVE_RUNTIME_DIR=/Users/levi/dev/worktrees/rive-candidate-s4 make scripted-golden-compare
```

The exact 0.732 C++ runner completes every corpus entry without a Luau abort.
The only runtime termination is the existing Rust/luaur `scope_probe` SIGTRAP,
which is an attributed comparison failure rather than a candidate-runner build
or execution failure.

## Version skew (product-risk order)

| surface | range check | finding | disposition |
| --- | --- | --- | --- |
| `.riv` header/format | `File::majorVersion/minorVersion` remain **7.2** | no header bump despite S4-38 adding new integer field encodings and layout/core types, renaming `NestedArtboardOrigin` to generalized `ComponentOrigin`, and S4-42 adding blob VM type keys 1043/1044; S4-47 adds `TextBlobAsset` editor schema and makes nested leaf `fit` bindable | treat S4-38/S4-42/S4-47 as one import-compatibility gate; regenerate schema and add exact old/new fixtures before a pin move |
| Layout solver | S4-38 changes Yoga from `rive_changes_v2_0_1_2` to `rive_changes_v2_0_1_2_grid` | grid/stack/participant behavior depends on a new fork and two additional Yoga sources; Nuxie uses retained Taffy layout, so solver semantics cannot be assumed equivalent | 10 **PORT** only as a fixture-driven vertical wave; prove flex regression controls plus grid/stack/participant fixtures |
| Luau bytecode/vendor | in-range pins progress `rive_0_730` → `rive_0_731` → final `rive_0_732` | exact 0.732 builds and its C++ runner completes all 321 scripted entries; luaur remains 0.1.8/0.724-based and still SIGTRAPs on `scope_probe`, while predecessor bytecode evidence shows its advertised range lags official Luau | 8 **WATCH**; retain the completed ratchet as a floor, inspect exact editor-emitted bytecode, and resolve the luaur compatibility gap before moving the vendor pin |
| Blob/data protocol | S4-42 adds `DataType::assetBlob = 14`; S4-45 inserts blob commands/messages into unversioned command enums | same-version queue/server peers stay aligned, but mixed binaries would shift later enum ordinals; Nuxie's queue/server are absent F3 | port S4-42 runtime data model; keep S4-45 WATCH until F3 has an explicit protocol/version story |
| Renderer shaders | S4-43 changes every dither-capable fragment path to bypass noise when alpha is zero | ordinary command comparison cannot observe the accumulated transparent-overdraw error; Nuxie carries generated WGSL and enables fixed/advanced dithering | port the alpha-zero guard with shader provenance regeneration and a renderer golden, independently of the Luau hunk |
| shaping/bidi | no HarfBuzz or SheenBidi revision changed | S4-21 remains the proven 5px text/layout signal; S4-38's solver change is Yoga skew, not shaping skew | keep exact text/layout corpus review; no shaping dependency action |
| image codecs | no image decoder dependency changed | S4-19 and S4-35 are runtime sizing/asset propagation fixes, not codec skew | add their upstream fixtures; no codec action |

## Resurfaced deferred/watch register

S3-1 and S3-3 remain S4-1 and S4-3 because `LAST_SYNCED_SHA` is still
`d788e8ec`; both remain at staleness 1. This top-up is part of the same manual
cycle, so it does not increment deferred staleness a second time. S3-2 remains
closed by D16.

| id | prior staleness | S4 staleness | surface | current evidence | exit criterion | recommendation |
| --- | ---: | ---: | --- | --- | --- | --- |
| `deferred-2026-07-19-luau-engine` | 2 | 3 | Luau VM/JIT/GC internals | exact 0.732 source now builds and completes the C++ scripted corpus; luaur remains 0.1.8/0.724-based and `scope_probe` still SIGTRAPs on its stack assertion | luaur publishes a compatible base or the final fork compatibility matrix is proved with editor-emitted bytecode in addition to the completed scripted ratchet | 8 **WATCH**; keep deferred |
| `deferred-2026-07-19-ore-gpu` | 2 | 3 | optional `RIVE_CANVAS && RIVE_ORE` scripted GPU host | Nuxie still has no ORE GPU host; S4-5 adds fixture asset IDs and S4-12 remains Unreal-only harness work | port the GPU host/context and lifetime model with a C++ oracle fixture when product content adopts ORE scripting | 4 **WATCH**; keep deferred |

## Final approval-ready priority order

This is the proposed execution order for explicit human approval. Ratings are
urgency/value scores from 1–10; dependency order deliberately keeps a lower
rated follow-up beside the foundational wave it requires.

| priority | id | rating | proposed action | ordering note |
| ---: | --- | ---: | --- | --- |
| 1 | S4-11 | 10 | PORT | deterministic first-advance crash guard |
| 2 | S4-13 | 10 | PORT | async scripting null-pointer safety |
| 3 | S4-26 | 10 | PORT | corrupt/truncated import hardening |
| 4 | S4-39 | 10 | PORT | text-run listener UAF prevention |
| 5 | S4-38 | 10 | PORT as one wave | foundational schema/layout/Yoga grid change with exact fixtures |
| 6 | S4-41 | 6 | PORT after S4-38 | retained layout-background optimization |
| 7 | S4-47 | 8 | PORT runtime/schema; SKIP editor hosts | layout-fit/blob protocol tail needed before S4-48 |
| 8 | S4-48 | 9 | PORT after S4-38/S4-47 | same-frame non-root layout-fit reflow |
| 9 | S4-42 | 10 | PORT as one wave | blob schema, runtime, scripting, and fixtures together |
| 10 | S4-3 | 9 | PORT | proven `scope_probe` compatibility gap |
| 11 | S4-14 | 9 | PORT | scripted converter hydration guard |
| 12 | S4-22 | 9 | PORT | host-bound view-model lifetime correctness |
| 13 | S4-35 | 9 | PORT | nested image/font identity propagation |
| 14 | S4-46 | 9 | PORT | scripted asset wrapper identity/lifetime |
| 15 | S4-24 | 8 | PORT | directly proven `data_bind_font_test` delta |
| 16 | S4-29 | 8 | PORT | solo data-binding correctness |
| 17 | S4-30 | 8 | PORT | directly proven `echo_show_demo` delta |
| 18 | S4-1 | 8 | PORT | directly proven ordinary/scripted `text_input` delta |
| 19 | S4-21 | 8 | PORT | directly proven ordinary/scripted text-layout deltas |
| 20 | S4-23 | 8 | PORT as reviewed wave | 96 ordinary/99 scripted attributed entries |
| 21 | S4-4 | 8 | PORT | global view-model unset support |
| 22 | S4-10 | 8 | PORT | stateful component bidirectional binding |
| 23 | S4-16 | 8 | PORT | virtualized scroll correctness |
| 24 | S4-17 | 8 | PORT | nested script view-model typing |
| 25 | S4-18 | 8 | PORT | paused nested-artboard opacity/layout propagation |
| 26 | S4-19 | 8 | PORT | computed image sizing |
| 27 | S4-7 | 7 | PORT | focus dirty propagation performance |
| 28 | S4-8 | 7 | PORT | script export tree-shaking with schema coverage |
| 29 | S4-43 | 8 | PORT shader; WATCH Luau pin | split alpha-zero dither fix from vendor move |
| 30 | S4-27 | 6 | PORT atom table; SKIP Unreal packaging | mixed product/build commit |
| 31 | S4-2 | 6 | SATISFIED | already landed independently as D16 |
| 32 | S4-15 | 8 | WATCH | Luau 0.730 pin superseded in-range |
| 33 | S4-25 | 8 | WATCH | Luau 0.731 pin superseded in-range |
| 34 | S4-31 | 5 | WATCH | retain only wgpu clip-plane portion with renderer evidence |
| 35 | S4-36 | 5 | WATCH | triangulation caching needs renderer counters/goldens |
| 36 | S4-5 | 4 | WATCH | ORE fixture metadata awaits an ORE corpus |
| 37 | S4-45 | 4 | WATCH | command-queue blob protocol awaits feature F3 |
| 38 | S4-28 | 3 | SKIP | Vulkan backend-specific fast path |
| 39 | S4-32 | 3 | SKIP | Wagyu/WebGL adapter outside Nuxie native wgpu path |
| 40 | S4-9 | 2 | SKIP | Metal backend capability only |
| 41 | S4-12 | 2 | SKIP | Unreal ORE harness only |
| 42 | S4-6 | 1 | SKIP | Unreal deploy harness only |
| 43 | S4-20 | 1 | SKIP | Incredibuild tooling only |
| 44 | S4-33 | 1 | SKIP | upstream rive_native test only |
| 45 | S4-34 | 1 | SKIP | Windows build tooling only |
| 46 | S4-37 | 1 | SKIP | Unreal RHI Vulkan only |
| 47 | S4-40 | 1 | SKIP | Unreal RHI MSAA only |
| 48 | S4-44 | 1 | SKIP | Unreal CI only |

## Approval-ready handoff

Phase S attribution is complete: every one of the 100 ordinary and 104
scripted final-cut failures maps to an upstream commit, and the prior 98
ordinary assignments were reverified unchanged. The report is ready to present
for row/group approval.

No approval is inferred by this closeout. Standing approvals remain `none`;
port nothing and move no pin until the orchestrator records an explicit
approval. Product runtime pins and `LAST_SYNCED_SHA` remain at `d788e8ec`, the
manifest inventory pin remains `b73bc675`, and `4ac7b327` remains a fetched
candidate only.

## S4 cycle closeout

The later user authorization approved all 30 rows in the final priority table.
All **30/30 approved PORT rows** are now complete, including the runtime/schema
subsets of the three split rows; their explicitly WATCH or SKIP portions remain
deferred or excluded as approved. The implementation train was:

| delivery | approved rows / scope |
| --- | --- |
| PR #195 (`levi/s4-ports-1`) | S4-11, S4-13, S4-26, S4-39 safety fixes |
| PR #196 (`levi/s4-ports-2`) | S4-38, S4-41, S4-47, S4-48 layout foundation and follow-ups |
| PR #199 (`levi/s4-ports-s4d`) | S4-7, S4-8, S4-16, S4-19, S4-27, S4-43 independent set |
| PR #201 (`levi/s4-ports-s4b`) | 12-row data-binding/view-model tranche |
| PR #202 (`levi/s4-ports-s4a`) | S4-42 blob wave |
| `levi/s4-ports-s4c` closeout | remaining shared-file ports, including S4-1, S4-21, and S4-23; corpus enrollment, attribution census, and pin advance |

Every current product/runtime pin, operational checkout instruction, and
`LAST_SYNCED_SHA` advanced to
`4ac7b32798da0482e441ef09304dc3b480ed3ee5`. The B6 audit pin, `rust_ref`
citations, historical sync records, port-manifest inventory cut, and Phase R
pixel-oracle revision remain deliberately unchanged.

The full closeout ratchet at the new pin is green:

- ordinary and scripted goldens: 353 entries, 324 exact, 670 exact segments,
  669 side-channel segments, 0 divergent, 0 unsupported-feature, 29 not-yet;
- `cargo test -p nuxie-runtime`: 978 passed, 0 failed;
- `cargo test -p nuxie --features scripting`: 506 passed, 0 failed;
- frame-loop checks: 111 checker tests; 456/456 C++ source rows classified,
  350 active frame-loop files (349 faithful, 1 approved divergence), 75 members
  (35 faithful, 40 adapted), 10 closed gaps, and scatter 154/154;
- test correspondence: 157 files and 1,404 upstream `TEST_CASE`s, with 5
  ported-differential, 38 ported-direct, 27 partial, 85 pending, and 2 n-a;
- Rust attribution: 10 checker tests and every in-scope Rust source classified.

No residual golden difference was re-expected. The pin probe exposed two
missed-attribution findings (`layout_text_match` fit-font measurement and
`data_binding_test` controlled-text alignment); both were ported with targeted
C++ differential coverage before the final zero-diff ratchet.

### Deferred rows after S4

Completing the cycle increments the two recurring deferrals and enrolls each
new S4 WATCH surface at staleness 1. The superseded S4-15/S4-25 vendor rows are
tracked by the single Luau-engine deferral, and S4-5 is tracked by the existing
ORE deferral, so they do not create duplicate queues.

| deferred id | source rows | staleness | disposition / exit signal |
| --- | --- | ---: | --- |
| `deferred-2026-07-19-luau-engine` | S4-15, S4-25, Luau portion of S4-43 | 4 | WATCH; keep the engine pin frozen until parity closeout is green, unless scripted bytecode evidence triggers the recorded early reopen |
| `deferred-2026-07-19-ore-gpu` | S4-5 | 4 | WATCH; require an ORE product surface, host/context lifetime model, and C++ oracle fixture |
| `deferred-2026-08-02-wgpu-clip-planes` | S4-31 wgpu subset | 1 | WATCH; require renderer evidence before adapting the optimization |
| `deferred-2026-08-02-triangulation-cache` | S4-36 | 1 | WATCH; require renderer counters and pixel goldens |
| `deferred-2026-08-02-command-queue-blob` | S4-45 | 1 | WATCH; reopen only with feature F3 and an explicit protocol/version story |

Standing approvals remain `none`. S4 authorization is exhausted and the
write-capable worker returns to fail-closed operation until a new cycle is
authorized.

### Post-closeout review note

The final standards/spec review fixed stale operational pin instructions, the
reported corpus-enrollment count, direct ownership for ComponentOrigin and the
three blob rows, and a self-validating post-FL-E8 count policy. It also found
one remaining source-attribution issue that the mechanical checker does not
reject: FLR-16 requires the four S4 layout owners (`grid_item_placement`,
`grid_track`, `layout_participant`, and `layout_sizing_style`) to be extracted
from `draw.rs` into direct Rust files before the closeout lands. This is
reported rather than silently waived; behavior and golden output remain green.
