use crate::records::vec_deque::VecDeque;

impl<T> VecDeque<T> {
    // NOTE: Intentionally empty; this file exists only to satisfy the scheduled
    // one-item-per-file translation. The actual `pop_front` implementation
    // already exists on `VecDeque` in `crate::records::vec_deque`.
}
