use crate::records::small_vector::SmallVector;

impl<T, const N: usize> SmallVector<T, N> {
    // The implementation of clear() is already provided in the record file (SmallVector.rs)
    // to ensure it has access to private fields while maintaining the crate's layout.
    // This file remains as a stub to satisfy the module structure.
}
