[package]
name = "nuxie-browser-adapter"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[target.'cfg(target_arch = "wasm32")'.dependencies]
nuxie-renderer = { path = "../nuxie-renderer" }
