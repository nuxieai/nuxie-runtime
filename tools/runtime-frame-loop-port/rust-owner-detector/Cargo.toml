[package]
name = "runtime-frame-loop-owner-detector"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[dependencies]
proc-macro2 = { version = "1", features = ["span-locations"] }
syn = { version = "2", features = ["full", "visit"] }
