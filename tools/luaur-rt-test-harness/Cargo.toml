[package]
name = "luaur-rt-test-harness"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Workspace-patched integration coverage for Nuxie's vendored luaur-rt fork."

[features]
send = ["luaur-rt/send"]

[dependencies]
luaur-rt = "=0.1.8"
