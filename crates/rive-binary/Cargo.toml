[package]
name = "rive-binary"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
anyhow = "1"
rive-schema = { path = "../rive-schema" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

