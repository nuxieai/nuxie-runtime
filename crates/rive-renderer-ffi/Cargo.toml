[package]
name = "rive-renderer-ffi"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = []
native = []

[dependencies]
rive-render-api = { path = "../rive-render-api" }

[build-dependencies]
cc = "1"
