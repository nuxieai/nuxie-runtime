# Upstream sync triage: `d788e8ec..d788e8ec`

Date: 2026-07-19

Manual cycle: 2 (clean completed cycle)

Upstream: `rive-app/rive-runtime`

Nuxie base: `origin/main` at
`91d52b77c0d1fdb92ac565f5596a5b5336e17dff`

Candidate: `d788e8ec6e8b598526607d6a1e8818e8b637b60c`
(`origin/main` and `runtime-v0.1.194` in the reference repository)

Authorization: the initial run stopped at the Phase S approval gate. The user
then explicitly approved the recommended focused local Luau hardening work,
cycle closeout if both ratchets became green, stationary pins, and continued
ORE deferral. There were no upstream port rows to authorize.

## Inventory

Both repositories were fetched before inventory. Nuxie started from the
refreshed `origin/main`; the reference repository's `origin/main` still
resolves to `d788e8ec`. Therefore `LAST_SYNCED_SHA..origin/main` contains
**0 commits and 0 changed files**.

| upstream sha | title | bucket | impact | risk | effort | corpus signal | recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| _none_ | no commits after `d788e8ec` | — | — | — | — | no candidate delta | no upstream action |

There are no new explicit-skip rows. Renderer remains an active bucket; it is
empty because the complete upstream range is empty.

## Candidate worktree and probes

All intended C++ candidate work used a clean throwaway detached worktree at
`d788e8ec6e8b598526607d6a1e8818e8b637b60c`. The user's pinned C++ checkout
was not checked out or reset, and both ratchets used the explicit temporary
path. Dependencies and outputs for those probes were bootstrapped in the
temporary worktree, which was removed at closeout.

During final report verification, a shell-quoting mistake unintentionally
invoked the scripted make target once without the explicit temporary path.
The pinned checkout remained at `7c778d13`, its reflog did not move, and it
has no tracked diff; the command refreshed ignored build outputs under its
existing local build tree. Those untracked/ignored artifacts were left intact
because pre-existing local build state could not be safely distinguished from
the refresh.

| probe | result | evidence |
| --- | --- | --- |
| default `make golden-compare` | **PASS** | `entries=317`, `exact=317`, `exact-segments=647`, `diverges=0`, `unsupported-feature=0`, `not-yet=0` |
| forced `make scripted-golden-compare` | **PASS** | same `317/317` exact entries and `647` exact segments; `scope_probe` exact; command exited 0 |

The first forced-scripted run exposed a deterministic local integration bug:
`scope_probe` trapped in luaur 0.1.8 with
`LUAU_ASSERT failed: L.top < L.ci.top`. A focused high-level scope test
reproduced the failure while the raw candidate bytecode-v7 load test passed,
separating bytecode decoding from scoped-module evaluation.

LLDB located the exhausted-slot assertions in luaur-rt's protected
`Table::set` trampoline, first while caching a loaded module and then while
installing `__index` on a fresh module-environment metatable. Both tables are
private plain tables whose semantics require no metamethod dispatch. Nuxie now
uses `raw_set` at those two sites, avoiding the trampoline's additional stack
slot without changing table behavior. The pre-existing focused scope test,
all `nuxie-scripting` tests, and the full forced-scripted corpus pass after the
change.

An unrelated authored-vector test fails identically on untouched
`origin/main` and on this change (`x=100`, expected `103`), so it is recorded
as pre-existing evidence rather than attributed to cycle 2.

## Version skew (product-risk order)

| surface | range check | finding | disposition |
| --- | --- | --- | --- |
| `.riv` header/format | no commits or file changes after `d788e8ec` | upstream remains at the already-synced 7.2 behavior | **no pin action** |
| Luau bytecode/vendor | no upstream dependency change; candidate remains on `rive_0_728_vec3`; Nuxie remains exactly pinned to `luaur-{common,rt,vm}=0.1.8` | real bytecode v7 and the full scoped-library/scripted corpus pass after the local private-table hardening; newer unexercised VM/JIT/GC semantics remain unavailable | **WATCH the engine dependency; cycle is unblocked** |
| shaping/layout/bidi | no HarfBuzz, Yoga, or SheenBidi changes | no new HarfRust, Taffy, or unicode-bidi skew | **no dependency action** |
| image codecs | no decoder or codec changes | no new image-crate skew | **no dependency action** |

## Resurfaced deferred/watch register

Both prior rows are resurfaced. Staleness advances from 0 to 1 because neither
dependency-level exit criterion has been met. The cycle-local Luau host crash
is fixed, so it no longer blocks closeout.

| id | staleness | surface | current evidence | exit criterion | recommendation |
| --- | ---: | --- | --- | --- | --- |
| `deferred-2026-07-19-luau-engine` | 1 cycle | Luau 0.725-0.728 VM/JIT/GC internals from `273da290`/`d1bf9128`/`dd6eb5c0` | luaur 0.1.8 remains the available engine; candidate bytecode v7, scoped libraries, and the full forced-scripted corpus now pass | re-audit when luaur publishes a newer Luau base; retain the real v7 fixture and full forced-scripted ratchet | 6 **WATCH**; keep deferred |
| `deferred-2026-07-19-ore-gpu` | 1 cycle | optional `RIVE_CANVAS && RIVE_ORE` scripted GPU host, including `GPUBuffer:write` source ranges from `dd6eb5c0` | Rust still has no GPUBuffer/GPUCanvas/ORE host context, and no corpus or public API exercises it | port the GPU host/context and lifetime model as one scoped feature, add a C++ oracle fixture, then implement and compare the range-write overload | 3 **WATCH**; continue deferral unless the product adopts ORE scripting |

## Closeout and pin audit

- No upstream code was ported because the candidate range is empty.
- The approved local Luau hardening changed only
  `crates/nuxie-scripting/src/vm.rs`.
- Every active runtime pin remains at `d788e8ec`; the historical Phase R CI
  override remains at `7c778d13`.
- `LAST_SYNCED_SHA` remains `d788e8ec`; clean manual cycle 2 is recorded.
- Both mandatory candidate ratchets are green at 317 exact files and 647 exact
  segments with zero divergences, unsupported features, or not-yet entries.
- After the approval-gate closeout, the user explicitly authorized activating
  the Phase S worker. It is active on its existing schedule, but its unchanged
  prompt fails closed while Standing approvals is `none`, so it may only
  return a blocker report. The read-only weekly drift scout remains active.

Manual cycle 2 is closed clean. No pins moved and both deferred rows remain in
the next-cycle queue at staleness 1.
