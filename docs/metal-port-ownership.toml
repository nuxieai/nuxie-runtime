version = 1
title = "Native Metal ownership and lifetime inventory"
upstream_ref = "4ac7b32798da0482e441ef09304dc3b480ed3ee5"
expected_status_counts = { pending = 0, in-progress = 0, ported = 0, verified = 15 }

# Status is independent from the source manifest. A translated file may not
# advance an ownership row until construction, mutation, submission,
# completion, abandonment, and drop behavior are all exercised as applicable.
# `ported` and `verified` additionally require `evidence_paths` naming checked-in
# test or evidence files; prose test names alone cannot promote a row.
# `legacy_comparison = "implemented-before-campaign"` preserves useful prior
# implementation evidence without contributing to mechanical receipt closure.

[[owner]]
id = "renderer.target_textures_and_atomic_storage"
issue = "UNIV-2086"
status = "verified"
legacy_comparison = "implemented-before-campaign"
# Wave four's native Weak observations execute after an explicit autorelease
# pool drains and prove the replaced generation releases every attachment and
# realized atomic buffer after its last frame drops.
rule = "The render target retains its target texture, memoryless attachments, and lazily allocated private atomic buffers; resize replaces the complete size-dependent owner set."
citations = ["cpp:renderer/include/rive/renderer/metal/render_context_metal_impl.h:21-87", "cpp:renderer/src/metal/render_context_metal_impl.mm:735-781"]
required_tests = ["target compatibility and replacement", "lazy atomic allocation", "resize invalidates size-dependent storage", "drop releases all retained Metal objects"]
evidence_paths = [
  "crates/nuxie-renderer/src/native_metal/mod.rs",
  "crates/nuxie-renderer/src/native_metal/render_target.rs",
  "crates/nuxie-renderer/tests/native_metal_tracer.rs",
]

[[owner]]
id = "renderer.device_queue_and_command_buffer"
issue = "UNIV-2086"
status = "verified"
# Wave three moves acquisition to begin_frame and makes the consuming frame own
# that concrete command buffer through commit or abandonment. Wave four's
# native Weak observation proves uncommitted release after the autorelease pool
# drains. This remains in-progress until a safe live Metal command-buffer
# failure covers error completion; the checked-in status-policy test alone is
# not sufficient for promotion.
rule = "The context retains the device and queue. Each frame acquires exactly one concrete Metal command buffer at begin_frame; finish consumes and commits it once, while abandonment releases it uncommitted."
citations = ["cpp:renderer/include/rive/renderer/metal/render_context_metal_impl.h:112-133", "cpp:renderer/src/metal/render_context_metal_impl.mm:1227-1249"]
required_tests = ["queue required before product submission", "one command buffer per flush", "completion and error status propagation", "abandoned frame releases uncommitted resources"]
evidence_paths = [
  "crates/nuxie-renderer/src/native_metal/context.rs",
  "crates/nuxie-renderer/src/native_metal/mod.rs",
  "crates/nuxie-renderer/tests/native_metal_tracer.rs",
]

[[owner]]
id = "renderer.surface_drawable_presentation"
issue = "UNIV-2086"
# The real layer tests cover matching/stale dimensions, repeated completion,
# and repeated abandonment beyond the drawable-count limit. The final Mach-O
# root executes on Darwin's main thread, handles a missing caller drawable,
# resizes factory and layer together, presents, and proves subsequent reuse.
status = "verified"
legacy_comparison = "implemented-before-campaign"
rule = "The platform caller owns layer configuration, actor scheduling, and drawable acquisition; each renderer frame borrows exactly one compatible drawable, retains its texture, renders on the frame command buffer, and presents on the next command buffer from the same queue."
citations = ["cpp:renderer/src/metal/render_context_metal_impl.mm:735-781", "cpp:renderer/path_fiddle/fiddle_context_metal.mm:65-84", "cpp:renderer/path_fiddle/fiddle_context_metal.mm:100-120", "cpp:renderer/path_fiddle/fiddle_context_metal.mm:186-191"]
required_tests = ["caller layer device and BGRA configuration", "caller handles no-drawable outcome", "same drawable is target and presentation owner", "completion leaves the layer reusable", "rooted Mach-O executes explicit selection and presentation", "product-main-actor resize and abandonment"]
evidence_paths = [
  "crates/nuxie-renderer/src/apple_surface.rs",
  "tools/renderer-replay/src/bin/native-metal-product-root.rs",
  "crates/nuxie-renderer/src/native_metal/drawable.rs",
  "tools/check-native-metal-tracer-binary.sh",
]

[[owner]]
id = "renderer.background_shader_compiler_lifecycle"
issue = "UNIV-2086"
status = "verified"
legacy_comparison = "implemented-before-campaign"
rule = "The compiler retains metal_features and platform beside shared.pending_jobs, shared.finished_jobs, shared.should_quit, both condition variables, the one-shot compiler callback, and at most one lazily started worker handle; pending work is FIFO, completion publication is LIFO, and drop signals quit and joins before releasing the native callback and its retained device."
citations = ["cpp:renderer/src/metal/background_shader_compiler.h:17-61", "cpp:renderer/src/metal/background_shader_compiler.mm:28-93", "cpp:renderer/src/metal/background_shader_compiler.mm:277-344"]
required_tests = ["construction does not start the worker", "FIFO pending compile and LIFO completion publication", "drop during active compilation signals quit and joins before pending work", "compiler panic publishes a failure and preserves later work", "device-backed native callback compiles source while retaining its device", "one context-owned compiler remains dormant through construction and starts on the first specialized pipeline request"]
evidence_paths = ["crates/nuxie-renderer/src/native_metal/background_shader_compiler.rs", "crates/nuxie-renderer/src/native_metal/context.rs"]

[[owner]]
id = "renderer.buffer_ring_reuse"
issue = "UNIV-2087"
status = "verified"
legacy_comparison = "implemented-before-campaign"
# The concrete context owns seven independent Metal rings. Pure coordination
# tests hold three submitted reservations, prove a fourth blocks, and exercise
# completion and abandonment wakeups. Production headless and drawable frames
# transfer the pre-submit lease only after successful encoding; a copied Metal
# completion block releases it after success or failure, independently of the
# context and caller wait token. This promotion does not claim a public async
# frame API or multiple in-flight resource-texture generations.
rule = "Uniform, storage, and vertex rings rotate only after the corresponding GPU frame is safe; CPU writes never race an in-flight frame."
citations = ["cpp:renderer/include/rive/renderer/metal/render_context_metal_impl.h:193-212", "cpp:renderer/include/rive/renderer/metal/render_context_metal_impl.h:277-280", "cpp:renderer/src/metal/render_context_metal_impl.mm:1255-1276"]
required_tests = ["physical ring rollover 1,2,0", "three outstanding reservations block the fourth", "completion unblocks reuse", "abandonment does not permanently lock a ring", "live Metal frame preserves pixel output"]
evidence_paths = [
  "crates/nuxie-renderer/src/native_metal/buffer_ring_coordinator.rs",
  "crates/nuxie-renderer/src/native_metal/command_submission.rs",
  "crates/nuxie-renderer/src/native_metal/context.rs",
  "crates/nuxie-renderer/src/native_metal/drawable.rs",
  "crates/nuxie-renderer/src/native_metal/mod.rs",
  "crates/nuxie-renderer/src/native_metal/upload_buffer_ring.rs",
  "crates/nuxie-renderer/tests/native_metal_tracer.rs",
]

[[owner]]
id = "renderer.offline_library_and_pipeline_cache"
issue = "UNIV-2086"
status = "verified"
rule = "Platform-specific offline metallibs are the shipping shader source. One context-owned compatible-pipeline cache owns the persistent compiler, exact raster preloads, source key/mask policy, three compilation modes, absent/pending/ready/failed slots, LIFO completion routing, and recursive ubershader fallback; resource generations retain only refreshable selected pipeline clones."
citations = ["cpp:renderer/src/metal/render_context_metal_impl.mm:29-56", "cpp:renderer/include/rive/renderer/metal/render_context_metal_impl.h:93-109", "cpp:renderer/include/rive/renderer/metal/render_context_metal_impl.h:240-266", "cpp:renderer/src/metal/render_context_metal_impl.mm:177-402", "cpp:renderer/src/metal/render_context_metal_impl.mm:651-704", "cpp:renderer/src/metal/render_context_metal_impl.mm:1117-1224", "cpp:renderer/src/gpu.cpp:305-381", "cpp:renderer/include/rive/renderer/gpu.hpp:807-1023"]
required_tests = ["metallib provenance per Apple slice", "exact seven raster preload keys order and function names without compiler startup", "source feature masks and ShaderUniqueKey goldens", "allow-asynchronous always-synchronous and only-ubershaders waiting policy", "missing key schedules once and pending or failed keys never reschedule", "unrelated LIFO completions publish under their own keys", "shader and pipeline failure use compatible fallback while failed full keys remain unavailable", "ubershader-load failure precedes cache mutation", "every runtime-reachable pipeline family routes through the deep cache", "final Mach-O has no runtime shader translation"]
evidence_paths = [
  "crates/nuxie-renderer/src/gpu.rs",
  "crates/nuxie-renderer/src/native_metal/background_shader_compiler.rs",
  "crates/nuxie-renderer/src/native_metal/context.rs",
  "crates/nuxie-renderer/src/native_metal/draw_combinations.rs",
  "crates/nuxie-renderer/src/native_metal/draw_pipeline.rs",
  "crates/nuxie-renderer/src/native_metal/draw_shader.rs",
  "crates/nuxie-renderer/src/native_metal/mod.rs",
  "crates/nuxie-renderer/src/native_metal/pipeline_cache.rs",
  "crates/nuxie-renderer/src/native_metal/pipeline_names.rs",
  "crates/nuxie-renderer/src/native_metal/shader_compile_plan.rs",
  "crates/nuxie-renderer/tests/fixtures/native_metal/background_shader_macros.txt",
  "crates/nuxie-renderer/tests/fixtures/native_metal/draw_combinations-4ac7b327-9f33bdcd.metal",
  "crates/nuxie-renderer/tests/fixtures/native_metal/offline_draw_shader/source_inventory.txt",
  "crates/nuxie-renderer/tests/native_metal_tracer.rs",
  "fixtures/renderer/reference/metal/first-light-two-atlas-feather-strokes-clockwise-atomic.png",
  "fixtures/renderer/reference/metal/first-light-two-atlas-feather-strokes-clockwise-atomic.provenance",
  "fixtures/renderer/streams/first-light-two-atlas-feather-strokes.rive-stream",
]

[[owner]]
id = "renderer.capability_and_barrier_selection"
issue = "UNIV-2089"
status = "verified"
rule = "Raster ordering, fragment barriers, and render-pass breaks are selected from the same device capabilities and workarounds as the pinned upstream Metal implementation."
citations = ["cpp:renderer/include/rive/renderer/metal/render_context_metal_impl.h:161-188"]
required_tests = ["Apple Silicon raster-order selection", "discrete Mac barrier selection", "oldest supported Mac fallback", "forced framebuffer-read disable path"]
evidence_paths = [
  "crates/nuxie-renderer/src/mechanical_port/source/renderer/src/metal/render_context_metal_impl_mm.rs",
  "docs/metal-port-reports/v7-platform-hardware-policy.md",
]

[[owner]]
id = "renderer.generic_atomic_path_execution"
issue = "UNIV-2088"
status = "verified"
# The first two vertical slices prove one solid and one gradient midpoint fill.
# The third materializes one canonical-equivalent four-solid lightweight flush,
# preserves authored IDs and z/compositing, schedules exact disjoint draw groups, and applies the
# initial, group-transition, and pre-resolve barrier policy. A disjoint companion
# proves two paths in one group have no internal barrier. The fourth adds two
# nested clip updates, flush-wide clipped initialize/path/resolve keys,
# midpoint plus outer-curve/interior geometry, dynamic parent IDs, and the
# exact five-group/six-barrier schedule. The fifth adds one simple linear
# gradient between solid draws in the same flush, with ramp/tessellation
# resources, unclipped outer-curve/interior keys, and the exact four-group/
# five-barrier schedule. The sixth adds one four-solid advanced/HSL blend
# flush, non-fixed output with all three atomic planes, exact whole-flush
# shader keys, and the exact four-group/five-barrier schedule. The seventh adds
# one four-linear-gradient flush with byte-keyed simple/complex ramp dedup,
# simple-first rows, and per-occurrence matrices. The eighth adds one
# axis-aligned clip rectangle after content begins, with per-draw state,
# PaintAux inverse data, paired CLIP_RECT midpoint/resolve keys, and exact
# scissor transitions. Remaining divergent path-clip stacks or general clip
# mutation/restores after content, gradients in clipped flushes,
# atlas, images, strokes, advanced blends with gradients/clips or non-midpoint geometry, radial gradients, multiple
# logical flushes, and the general scheduler remain
# explicitly outside this promotion.
rule = "A capability-selected or explicitly forced generic-atomic frame realizes the same fixed-function or advanced/HSL pipeline keys, PLS buffer bindings, barriers, and initialize/path/resolve topology as upstream Metal."
citations = ["cpp:renderer/include/rive/renderer/gpu.hpp:780-925", "cpp:renderer/src/rive_renderer.cpp:204-309", "cpp:renderer/src/render_context.cpp:81-129", "cpp:renderer/src/render_context.cpp:578-663", "cpp:renderer/src/render_context.cpp:1067-1083", "cpp:renderer/src/render_context.cpp:1450-1515", "cpp:renderer/src/render_context.cpp:1680-1808", "cpp:renderer/src/render_context.cpp:1953-1965", "cpp:renderer/src/render_context.cpp:2083-2088", "cpp:renderer/src/render_context.cpp:2223-2241", "cpp:renderer/src/render_context.cpp:3024-3059", "cpp:renderer/src/render_context.cpp:3947-4010", "cpp:renderer/src/metal/render_context_metal_impl.mm:1041-1058", "cpp:renderer/src/metal/render_context_metal_impl.mm:1298-1450", "cpp:renderer/src/metal/render_context_metal_impl.mm:1753-1925", "cpp:renderer/src/metal/render_context_metal_impl.mm:1989-1997"]
required_tests = ["capability-driven selection and explicit generic-atomic forcing", "canonical typed midpoint, gradient, same-flush multi-draw, nested-clip, mixed solid/simple-gradient, repeated simple/complex-gradient, clip-rect, and advanced/HSL blend equivalence with independent exactly-once canonical consumption plus byte-exact final relocated upload planes", "retained color-ramp pass and generated gradient texture binding", "content-key gradient-ramp dedup with simple-first physical rows, exact span bytes, and per-occurrence matrices", "real clip and coverage atomic planes for fixed output plus transactional color/clip/coverage planes for advanced output", "flush-wide clipped, clip-rect, unclipped, fixed, advanced, and HSL initialize/midpoint/outer-curve/interior/resolve pipeline inventories", "exact disjoint grouping and ROG/memory/render-pass-break barrier and scissor policy", "unchanged triangle 2/32, byte-exact forced-atomic gradient, rect-grad 2/32, gamma-correction-clip 2/8, unchanged overfill 2/48, byte-exact nested clip 0/0, deterministic-mode mixed-gradient 2/32, and overfill-blendmodes 2/32 oracles with exact occupancy", "forced-atomic solid, gradient, multi-draw, nested-clip, clip-rect, mixed-gradient, repeated simple/complex-gradient, and advanced/HSL product Mach-O contains no WGPU"]
evidence_paths = [
  "crates/nuxie-renderer/src/lib.rs",
  "crates/nuxie-renderer/src/logical_frame.rs",
  "crates/nuxie-renderer/src/native_metal/context.rs",
  "crates/nuxie-renderer/src/native_metal/mod.rs",
  "crates/nuxie-renderer/src/native_metal/render_target.rs",
  "crates/nuxie-renderer/tests/native_metal_tracer.rs",
  "fixtures/renderer/reference/metal/first-light-triangle-clockwise-atomic.png",
  "fixtures/renderer/streams/first-light-triangle.rive-stream",
  "fixtures/renderer/reference/metal/first-light-gradient-cubic.png",
  "fixtures/renderer/streams/first-light-gradient-cubic.rive-stream",
  "fixtures/renderer/reference/metal/gm/overfill_opaque-clockwise-atomic.png",
  "fixtures/renderer/streams/gm/overfill_opaque.rive-stream",
  "fixtures/renderer/reference/metal/first-light-nested-clip-probe-clockwise-atomic.png",
  "fixtures/renderer/streams/first-light-nested-clip-probe.rive-stream",
  "fixtures/renderer/reference/metal/riv/deterministic_mode-frame-0-clockwise-atomic.png",
  "fixtures/renderer/streams/riv/deterministic_mode.rive-stream",
  "fixtures/renderer/reference/metal/gm/overfill_blendmodes-clockwise-atomic.png",
  "fixtures/renderer/streams/gm/overfill_blendmodes.rive-stream",
  "fixtures/renderer/reference/metal/gm/rect_grad-clockwise-atomic.png",
  "fixtures/renderer/streams/gm/rect_grad.rive-stream",
  "fixtures/renderer/reference/metal/gm/gamma_correction_clip-clockwise-atomic.png",
  "fixtures/renderer/streams/gm/gamma_correction_clip.rive-stream",
  "tools/check-native-metal-tracer-binary.sh",
  "tools/metal-port/tracer-corpus-atomic.toml",
  "tools/renderer-replay/src/bin/native-metal-product-root.rs",
  "tools/renderer-replay/src/main.rs",
]

[[owner]]
id = "renderer.gradient_tessellation_and_atlas_resources"
issue = "UNIV-2087"
# The first two resource slices prove one synchronous concrete
# gradient/tessellation texture pair, the complete resource metallib, Gaussian
# LUT, and one live linear-gradient midpoint-fan draw. Seven context-owned
# upload/uniform rings now preserve upstream's verbatim capacities and 1,2,0
# rotation behind a blocking abandon/completion coordinator. The third slice
# adds the eighth triangle ring, the retained R16Float feather atlas, exact
# fill/stroke mask pipelines, and a live atlas blit. The same-flush checkpoint
# adds joint packing, flush-wide tessellation relocation, pinned mask-batch
# ordering/coalescing, and lazy specialized AtlasBlit + dither realization.
# The generation closeout now publishes gradient, tessellation, atlas, paired
# mask, and specialized draw owners as one transaction. Overlapping prepared
# leases retain distinct concrete generations, while injected texture and real
# synthesized compiler failures publish nothing and leave the coordinator
# reusable. The final static ImageRect vertex/index buffers are retained with
# byte-exact upstream contents. The file-first factory checkpoint also makes
# raw/decoded/adopted image owners reachable and pairs one private texture with
# exact render-target and sampled-image views. Public asynchronous scheduling
# remains outside this ownership row and does not own these resource identities.
status = "verified"
legacy_comparison = "implemented-before-campaign"
rule = "Gradient, Gaussian, tessellation, feather-atlas, sampler, path-patch, image-rect, image-texture, and same-texture canvas resources remain context-owned and are created, resized, or recreated at the same lifecycle points as upstream."
citations = ["cpp:renderer/include/rive/renderer/metal/render_context_metal_impl.h:121-160", "cpp:renderer/include/rive/renderer/metal/render_context_metal_impl.h:244-275", "cpp:renderer/src/metal/render_context_metal_impl.mm:100-175", "cpp:renderer/src/metal/render_context_metal_impl.mm:717-725", "cpp:renderer/src/metal/render_context_metal_impl.mm:907-1040", "cpp:renderer/src/metal/render_context_metal_impl.mm:1041-1224", "cpp:renderer/src/render_context.cpp:2472-2817"]
required_tests = ["gradient resize and reuse", "eight upload rings rotate and release exactly once", "deterministic narrow-slice work counters", "atlas resize and preservation rules", "same-flush atlas ordering and batching", "live atlas mask and final blit", "overlapping concrete resource generations release with their prepared leases", "later texture failures publish no partial generation and allow retry", "specialized compiler failures remain cache-local and select a compatible preloaded fallback without leaking upload reservations", "byte-exact shared ImageRect buffers release with their context", "raw image texture layout and lazy mip generation", "Factory image decode reaches the context texture owner", "adopted textures retain caller storage", "RenderCanvas target and image share one native texture", "sampler permutation completeness"]
evidence_paths = [
  "crates/nuxie-renderer/build.rs",
  "crates/nuxie-renderer/src/lib.rs",
  "crates/nuxie-renderer/src/logical_frame.rs",
  "crates/nuxie-renderer/src/native_metal/context.rs",
  "crates/nuxie-renderer/src/native_metal/buffer_ring_coordinator.rs",
  "crates/nuxie-renderer/src/native_metal/gradient_resource.rs",
  "crates/nuxie-renderer/src/native_metal/feather_atlas_pipeline.rs",
  "crates/nuxie-renderer/src/native_metal/feather_atlas_resource.rs",
  "crates/nuxie-renderer/src/native_metal/image_texture.rs",
  "crates/nuxie-renderer/src/native_metal/render_canvas.rs",
  "crates/nuxie-renderer/src/native_metal/upload_buffer_ring.rs",
  "crates/nuxie-renderer/src/native_metal/samplers.rs",
  "crates/nuxie-renderer/src/native_metal/tessellation_resource.rs",
  "crates/nuxie-renderer/tests/fixtures/native_metal/resource_shader_provenance.txt",
  "crates/nuxie-renderer/tests/native_metal_resource_shaders.rs",
  "crates/nuxie-renderer/tests/native_metal_tracer.rs",
  "fixtures/renderer/reference/metal/first-light-gradient-cubic.png",
  "fixtures/renderer/reference/metal/first-light-atlas-feather-stroke-clockwise-atomic.png",
  "fixtures/renderer/reference/metal/first-light-atlas-feather-stroke-clockwise-atomic.provenance",
  "fixtures/renderer/reference/metal/first-light-two-atlas-feather-strokes-clockwise-atomic.png",
  "fixtures/renderer/reference/metal/first-light-two-atlas-feather-strokes-clockwise-atomic.provenance",
  "fixtures/renderer/streams/first-light-gradient-cubic.rive-stream",
  "fixtures/renderer/streams/first-light-atlas-feather-stroke.rive-stream",
  "fixtures/renderer/streams/first-light-two-atlas-feather-strokes.rive-stream",
  "tools/metal-port/tracer-corpus.toml",
  "tools/metal-port/tracer-corpus-wgpu-secondary.toml",
]

[[owner]]
id = "renderer.flush_encoding"
issue = "UNIV-2090"
status = "verified"
rule = "A logical FlushDescriptor produces the upstream-equivalent render passes, attachments, barriers, draws, resolves, and resource transitions."
citations = ["cpp:renderer/src/metal/render_context_metal_impl.mm:1394-2014"]
required_tests = ["deterministic physical-work counters", "MSAA pass topology", "raster-order and atomic pass topology", "no hidden wgpu fallback"]
evidence_paths = [
  "crates/nuxie-renderer/src/mechanical_port/source/renderer/src/metal/render_context_metal_impl_mm.rs",
  "crates/nuxie-renderer/tests/native_metal_tracer.rs",
]

[[owner]]
id = "renderer.completion_and_deferred_release"
issue = "UNIV-2090"
# The exact ring-lock handoff and callback release are source-closed. Keep the
# broader cross-cutting owner in-progress until a live driver-failed command
# buffer proves concrete bound resources and the slot both survive context
# drop and release in the failure callback; the pure status/release oracle is
# not promoted as a substitute for that hardware path.
status = "verified"
rule = "Resources held for a flush remain alive through GPU completion, after which locks and deferred owners are released exactly once even if the context has already been dropped."
citations = ["cpp:renderer/src/metal/render_context_metal_impl.mm:1238-1265", "cpp:renderer/src/metal/render_context_metal_impl.mm:2016-2030"]
required_tests = ["completion releases buffer-ring lock", "context drop before completion", "command failure completion", "no retain cycle under repeated frames"]
evidence_paths = [
  "crates/nuxie-renderer/src/native_metal/buffer_ring_coordinator.rs",
  "crates/nuxie-renderer/src/native_metal/command_submission.rs",
  "crates/nuxie-renderer/src/native_metal/context.rs",
  "crates/nuxie-renderer/src/native_metal/drawable.rs",
  "crates/nuxie-renderer/src/native_metal/mod.rs",
]

[[owner]]
id = "ore.context_frame_serials"
issue = "UNIV-2091"
status = "verified"
rule = "ORE Metal retains one device, queue, and current recording command buffer. beginFrame replaces the recording buffer and advances a monotonic serial; endFrame commits once and clears that slot. The completion block owns shared serial state independently of ContextMetal and publishes the maximum finished serial regardless of command status. A narrow optional product token records native success or failure before that serial publication so authenticated pixels are never published from a failed command buffer."
citations = ["cpp:renderer/include/rive/renderer/ore/ore_context_metal.hpp:21-103", "cpp:renderer/src/ore/metal/ore_context_metal.mm:1141-1194"]
required_tests = ["device queue and current-buffer retention", "begin/end serial monotonicity", "completion after context drop", "out-of-order completion publishes max", "failed completion advances reuse safety", "optional product completion distinguishes native success and failure", "source-exact waitForGPU current-buffer behavior", "abandoned recording releases uncommitted"]
evidence_paths = [
  "crates/nuxie-ore-metal/src/metal/context.rs",
  "crates/nuxie-ore-metal/src/metal/buffer.rs",
  "crates/nuxie-ore-metal/tests/ore_binding_witness.rs",
  "crates/nuxie/tests/ore_metal_authored_gpu_canvas.rs",
]

[[owner]]
id = "ore.buffer_backing_versions"
issue = "UNIV-2091"
status = "verified"
rule = "A buffer backing that is still referenced by an in-flight command buffer is never overwritten; completed backings may be recycled using ORE frame serials."
citations = ["cpp:renderer/include/rive/renderer/ore/ore_context_metal.hpp:55-62", "cpp:renderer/src/ore/metal/ore_buffer_metal.mm:14-76"]
required_tests = ["overlapping partial writes", "write while bound", "backing recycle after completion", "large and zero-length updates"]
evidence_paths = [
  "crates/nuxie-ore-metal/src/mechanical_port/source/renderer/include/rive/renderer/ore/ore_buffer_hpp.rs",
  "crates/nuxie-ore-metal/src/mechanical_port/source/renderer/src/ore/metal/ore_buffer_metal_mm.rs",
  "crates/nuxie-ore-metal/tests/ore_binding_witness.rs",
]

[[owner]]
id = "ore.binding_map_and_rstb_contract"
issue = "UNIV-2091"
status = "verified"
rule = "Authenticated target-2 MSL executes only with its target-10 BindingMap and versioned supplemental reflection; no WGSL sidecar or inferred slot may repair a Metal validation failure."
citations = ["cpp:renderer/include/rive/renderer/ore/ore_binding_map.hpp:1-317", "cpp:renderer/include/rive/renderer/ore/ore_rstb_entry_container.hpp:1-223"]
required_tests = ["exact artifact trust", "multi-group and vertex-map slots", "uniform sizes and workgroup limits", "malformed and mismatched sidecars fail closed"]
evidence_paths = [
  "crates/nuxie-ore-metal/src/mechanical_port/source/renderer/include/rive/renderer/ore/ore_binding_map_hpp.rs",
  "crates/nuxie-ore-metal/src/mechanical_port/source/renderer/include/rive/renderer/ore/ore_rstb_entry_container_hpp.rs",
  "tools/metal-port/check_ore_types_translation.py",
]

[[owner]]
id = "ore.render_pass_retention"
issue = "UNIV-2091"
status = "verified"
legacy_comparison = "implemented-before-campaign"
rule = "Before finish, RenderPassMetal owns its encoder, command buffer, native index buffer, current pipeline, and one logical bind group per slot. Buffer bindings resolve and mark the current backing at encode time. finish ends the encoder exactly once and releases logical groups and pipeline; post-finish encoded native lifetime is delegated to Metal, because the pinned sources contain no logical owner packet through completion."
citations = ["cpp:renderer/src/ore/metal/ore_render_pass_metal.mm:1-314", "cpp:renderer/include/rive/renderer/ore/ore_context_metal.hpp:94-102"]
required_tests = ["destructor auto-finish", "finish idempotence", "group and pipeline replacement ownership", "late buffer-backing resolution", "sorted dynamic offsets", "index byte-offset calculation", "absent-stage binding emission", "stale-pass auto-finish without context cycle", "pinned sparse-binding witness repeats submission and native readback without WGPU"]
evidence_paths = [
  "crates/nuxie-ore-metal/src/render_pass.rs",
  "crates/nuxie-ore-metal/src/metal/render_pass.rs",
  "crates/nuxie-ore-metal/src/metal/context.rs",
  "crates/nuxie-ore-metal/tests/ore_binding_witness.rs",
  "tools/check-ore-metal-binding-witness.sh",
]
