[package]
name = "nuxie-image-codec"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
image-webp = "0.2.4"
jpeg-decoder = { version = "0.3", default-features = false, features = ["rive_v9f"] }
moxcms = "0.8.1"
png = "0.18"

[dev-dependencies]
nuxie-render-stream = { path = "../nuxie-render-stream" }
