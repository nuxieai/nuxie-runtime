# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "luaur-compiler"
version = "0.1.8"
authors = ["Pawel Jankiewicz <p.jankiewicz@gmail.com>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Luau source-to-bytecode compiler (Rust)."
readme = false
keywords = [
    "luau",
    "lua",
    "compiler",
    "bytecode",
]
categories = ["compilers"]
license = "MIT"
repository = "https://github.com/pjankiewicz/luaur"

[features]
cli = ["native"]
default = ["std"]
native = ["std"]
native-codegen = ["native"]
std = []
wasm = ["dep:wasm-bindgen"]

[lib]
name = "luaur_compiler"
crate-type = ["rlib"]
path = "src/lib.rs"

[[bin]]
name = "luau_compile"
path = "src/bin/luau_compile.rs"

[dependencies.luaur-ast]
version = "0.1.8"

[dependencies.luaur-bytecode]
version = "0.1.8"

[dependencies.luaur-common]
version = "0.1.8"

[dependencies.wasm-bindgen]
version = "0.2"
optional = true
