//! Node: `cxx:TypeAlias:Luau.Compiler:Compiler/include/luacode.h:12:lua_CompileOptions`
//!
//! C forward-declaration `typedef struct lua_CompileOptions lua_CompileOptions;`
//! — the same type as the struct definition, so this is a transparent alias to
//! the record rather than a second type.

pub type LuaCompileOptions = crate::records::lua_compile_options::LuaCompileOptions;
