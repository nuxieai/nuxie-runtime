# Shared renderer authority translation closeout

Date: 2026-08-22

Pinned upstream: `4ac7b32798da0482e441ef09304dc3b480ed3ee5`

Result: **GREEN; the Vulkan queue may open after this barrier is checkpointed**

## Closed denominator

- 87/87 shared shader/build source owners have unique translation receipts.
- 72/72 shared ownership units are complete in frozen dependency order.
- 78/78 shader inputs are retained as byte-exact pinned programs in compiled
  Rust owners; no legacy-WGPU shader or rewritten source is substituted.
- The two renderer build files and three dependency bootstrap scripts retain
  byte-exact source snapshots and explicit source-shaped configuration,
  command, revision, failure, and platform contracts.
- The shader Makefile and minifier independently compile their complete prior
  Rust translations against the current pinned source bytes. The SPIR-V and
  WGSL header generators retain their exact source plus authored transformation
  and failure contracts.
- All 520 frozen generated outputs remain governed by
  `docs/backend-port-generated-artifacts.tsv`; generated bytes are immutable
  translation inputs, not candidate-derived artifacts.

## Enforced gate

`make backend-port-shader-authority-check` regenerates the complete 78-owner
shader tranche in memory and fails on any missing or changed target, snapshot,
receipt, dependency list, source hash, or module inventory.

`make backend-port-translation-check` independently binds every receipt to the
frozen ownership and dependency ledgers, the pinned upstream bytes, the exact
compiled target hash, and the exact in-repository source snapshot. It rejects
partial ownership units, dependency skips, target overlap, exclusions,
evidence-only files, and campaigns ahead of the active queue.

The shared authority adds no product route, fallback, fixture-selected
behavior, or backend abstraction. Vulkan, WebGPU, and WebGL2 runtime owners
remain separate pending campaigns. The legacy Rust-WGPU implementation remains
untouched until all three campaigns and global closeout gates pass.
