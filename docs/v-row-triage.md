# V11-V40 triage: cluster table and per-row evidence

Evidence captures: vtriage-capture/ (per-row cpp/rust streams). 26 runtime divergences in 11 clusters; V22, V27, V35, V36 are harness/stale-capture artifacts; V12 is a Rust convergence timeout.

| Cluster | V-rows | Suspected pinned C++ mechanism | Upstream file:line | Fix size |
|---|---:|---|---|:---:|
| Retained renderer-resource identity across frames | V13, V15, V23, V24, V26, V29, V31, V40 (8) | A live occurrence owns one `RenderPaint`/`RenderPath`; dirt rewinds and refills it rather than creating a new backend object, and the object remains addressable through later frames. Rust instead rematerializes paths/gradients in six rows and loses the retained paint lookup in two. | `src/shapes/paint/shape_paint.cpp:50-57,173-185`; `src/shapes/paint/shape_paint_path.cpp:58-73`; `src/shapes/paint/shape_paint_mutator.cpp:7-25` | L |
| Stateful nested-artboard/VMI propagation | V20, V21, V34, V38 (4) | The active stateful VMI is bound to the mounted child before its nested state machine consumes the child's `DataContext`; artboard swaps rebuild the occurrence and its state-machine binding as one operation. | `src/nested_artboard.cpp:156-185,228-300` | L |
| `ArtboardComponentList` occurrence advance, transform, and keep-going composition | V14, V18, V19, V39 (4) | List children advance in index order, nested state transitions are conditionally retried on settle passes, child dirt bubbles to the list, and virtualization reuses occurrence/state-machine pairs. V14/V19/V39 currently differ only in the raw `advanceAndApply` return; V18 first differs in the mounted-child transform. | `src/artboard_component_list.cpp:715-814,827-885,1660-1725`; `src/animation/state_machine_instance.cpp:2649-2707` | L |
| Scripted path-effect invalidation and feather composition | V25, V30 (2) | A scripted effect advances once per active frame, invalidates its retained effect path, then `ShapePaint::draw` selects the last effect path and rebuilds the inner-feather path only when that effect changed. | `src/scripted/scripted_path_effect.cpp:111-132,199-207`; `src/shapes/paint/shape_paint.cpp:115-152` | M |
| Mid-frame VM comparator evaluation | V11, V33 (2) | `ConditionComparisonSelf` observes `source->hasChanged()` and suppresses a change already consumed by the same layer. It must be evaluated between the same data-bind/update passes as C++. V33's property-vs-trigger comparator is explicitly lowered to this same mechanism. | `src/animation/transition_viewmodel_condition.cpp:49-60,1098-1108`; settle site `src/animation/state_machine_instance.cpp:2665-2697` | M |
| Data-bind/frame-component convergence cost | V12 (1) | C++ bounds the update/data-bind/transition settlement loop to five outer passes and resets the data context after each pass. The Rust full run spends 173.40 seconds of user CPU and still does not complete by 180 seconds. | `src/animation/state_machine_instance.cpp:2649-2707`; `src/artboard.cpp:1417-1480` | L |
| Animated compound/feather path interpolation | V16 (1) | Keyed values are applied in authored order before dependency settlement; feather/effect paths are rebuilt from the settled source path and transform. | `src/animation/linear_animation.cpp:71-84`; `src/shapes/paint/feather.cpp:36-89` | M |
| Skin/constraint dependency order | V17 (1) | A `Skin` depends on every tendon bone and its peer constraints, snapshots all bone transforms in dependency order, then deforms vertices from that buffer. | `src/bones/skin.cpp:38-64,79-94` | L |
| Keyed callback interval and listener event reporting | V28 (1) | Animation advance reports callbacks over the exact `(lastTime, newTime]` interval, including loop-wrap segments; keyed-property lookup computes delay and reports in authored order. | `src/animation/linear_animation_instance.cpp:208-217,260-307`; `src/animation/keyed_property.cpp:54-97` | M |
| Scripted `Node.asPath()` snapshot retention | V32 (1) | `asPath` exposes the current authored `Path::rawPath()` to the script draw call, and the scripted drawable advances/marks paint dirt before drawing. | `src/lua/lua_artboards.cpp:884-900`; `src/scripted/scripted_drawable.cpp:19-73,376-399` | M |
| Animated vertical-trim text layout | V37 (1) | Top/bottom trim is computed from shaped line metrics, feeds both measured bounds and render placement, and a trim-property change dirties the shape/layout path. | `src/text/text.cpp:226-290,364-386,644-647,1403-1408` | M |
| Row | First observed divergent operation at the enrolled samples | Failing subsystem / disposition |
|---|---|---|
| V11 | At t=0, `advance ... statesChanged=1` (Rust) vs `2` (C++); without side-channel, the first draw mismatch is the later layout path width `279.087891` vs `263.087891`. | VM comparator evaluation vs data-bind/settle ordering. |
| V12 | C++ completes all samples with a 2,246,690-byte stream. Rust exits 124 at 180.05s, using 173.40s user CPU and emitting no completed stream. A t=0-only run completes within 180s, isolating the failure to the added post-zero work. | Data-bind/frame-component convergence or pathological post-zero complexity. |
| V13 | At t=0.5 Rust inserts `makeEmptyRenderPath id=9` before drawing the filled compound path; C++ reuses retained path id 7 directly. | Render-path identity/cache invalidation. The annotation's “empty drawn path” wording is not what the fresh stream shows. |
| V14 | At t=1, Rust reports `settled=false`; C++ reports `settled=true`. With side-channel off, draw streams are contract-exact. | Component-list keep-going/settle return only; the recorded 724-vs-1074 clip mismatch does not reproduce. |
| V15 | Before the t=2 `advance` record, Rust starts creating gradients (first id 14); C++ has no corresponding live occurrence/resource creation. | Post-zero retained paint/occurrence realization. |
| V16 | At t=2, `drawPath` id 54 first differs by about 0.01406 in the animated compound points. | Animated compound/feather path interpolation. |
| V17 | At t=0.5, stroke path id 19 endpoints are `(6.45296,-6.45239)..(10.4287,-10.4278)` vs C++ `(15.2899,-15.2885)..(15.3052,-15.3038)`. | Skin/constraint update order. |
| V18 | At t=0.5, Rust applies translation `(1121,259)` where C++ applies identity. | Mounted component-list child/draw-order transform. |
| V19 | At t=1, Rust reports `settled=false`; C++ reports `settled=true`. With side-channel off, draw streams are contract-exact. | Component-list child-origin keep-going/settle return only. |
| V20 | At t=1, the first ellipse is radius 30 in Rust and radius 50 in C++. | Stateful nested VMI number propagation. |
| V21 | At t=1, Rust applies scale `-1.5`; C++ applies `+1.5`. | Stateful nested VMI binding/typed comparator consumption. |
| V22 | No divergence under the current contract; 147 lines align and the maximum numeric delta is `3.9e-6`. | **Artifact; see below.** |
| V23 | Before the t=0.5 `advance` record, Rust creates gradient id 24 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V24 | t=0 succeeds; adding t=0.5 fails during Rust renderer synchronization with `missing render paint for global 584`. | Retained render-paint lookup/lifetime. |
| V25 | At t=1, draw path id 2 has a radically different scripted group/effect result (C++ line has 9,515 chars vs Rust 2,461). | Scripted group/path-effect invalidation. |
| V26 | Before the t=0.5 `advance` record, Rust creates gradient id 189 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V27 | Byte-identical 417,801-byte streams from both runners. | **Artifact; see below.** |
| V28 | At t=1 C++ reports `main-event-2` with delay `0.183333337`; Rust advances to semantics with no event. The no-side draw later also differs (Rust creates a fresh empty path where C++ draws retained path id 7). | Keyed callback interval/listener event timing; downstream retained-path consequence. |
| V29 | t=0 succeeds; adding t=0.5 fails during Rust renderer synchronization with `missing render paint for global 71`. | Retained render-paint lookup/lifetime. |
| V30 | At t=0.5, draw path id 5 has different effect/feather points (first material delta: `(49.5722,6.52631)` vs Rust `(49.8774,27.9455)`). | Scripted path-effect advance and feather composition. |
| V31 | Before the t=0.5 `advance` record, Rust creates gradient id 50 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V32 | At t=0.5, Rust draws empty path id 7; C++ draws the authored seven-segment closed path. | Scripted `asPath` source/snapshot retention. |
| V33 | At t=0.5, Rust remains red `0xffff0000`; C++ applies green `0xff07fb5a`. | Trigger-valued `ConditionComparisonSelf`, sharing V11's comparator mechanism. |
| V34 | At t=0.5, path geometry is identical at the first differing line, but Rust paint is red and C++ paint is black. | Stateful nested VMI color propagation. The annotation's “first path geometry mismatch” does not reproduce. |
| V35 | Without side-channel the two 2,319-byte streams are byte-identical. With side-channel, C++ segfaults before emitting a stream on three sequential reproductions, including t=0-only. | **Artifact/harness-path failure; see below.** |
| V36 | No divergence under the current contract; 2,285 lines align and the maximum numeric delta is `3.0e-5`. | **Artifact; see below.** |
| V37 | At t=0.5, Rust text translation is y=`187.584229`; C++ is y=`182.76001` (delta 4.824219). | Animated vertical-trim shaping/layout propagation. The numeric pair in the register is stale. |
| V38 | At t=0.5, draw path id 3 first differs in the selected child artboard's later glyph/path coordinates. | Scripted VM-instance-to-artboard selection and nested state propagation. |
| V39 | **Closed:** draw and side-channel streams are exact at t=0/2/4. | Pooled row restoration now preserves concrete renderer owners. |
| V40 | Before the t=1 `advance` record, Rust creates gradient id 30 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| Row | Why it is not the recorded runtime divergence on this fresh pin |
|---|---|
| V22 | The fresh side-channel comparison passes. The streams have identical structure and only `3.9e-6` maximum numeric drift, far inside `1.3e-4`; the recorded 245x250-vs-490x362.5 layout mismatch is absent. This is consistent with a stale runner/capture used during densification, not the current fixture and pin. |
| V27 | The fresh runner outputs are byte-identical, including image buffer commands. The registered “buffer command phase” failure is absent. |
| V35 | The claimed radius 100-vs-75 draw divergence is absent: no-side streams are byte-identical. Enabling the diagnostic side-channel deterministically segfaults the C++ golden runner before t=0 output, so this row currently diagnoses a side-channel/semantics harness path, not the recorded Rust draw bug. The source `.riv` is clean at the pinned upstream commit. |
| V36 | The fresh side-channel comparison passes. The streams have identical structure and only `3.0e-5` maximum numeric drift; the recorded empty-path-vs-compound-path failure is absent. This is also consistent with stale runner/capture evidence. |
| Cluster | V-rows | Suspected pinned C++ mechanism | Upstream file:line | Fix size |
|---|---:|---|---|:---:|
| Retained renderer-resource identity across frames | V13, V15, V23, V24, V26, V29, V31, V40 (8) | A live occurrence owns one `RenderPaint`/`RenderPath`; dirt rewinds and refills it rather than creating a new backend object, and the object remains addressable through later frames. Rust instead rematerializes paths/gradients in six rows and loses the retained paint lookup in two. | `src/shapes/paint/shape_paint.cpp:50-57,173-185`; `src/shapes/paint/shape_paint_path.cpp:58-73`; `src/shapes/paint/shape_paint_mutator.cpp:7-25` | L |
| Stateful nested-artboard/VMI propagation | V20, V21, V34, V38 (4) | The active stateful VMI is bound to the mounted child before its nested state machine consumes the child's `DataContext`; artboard swaps rebuild the occurrence and its state-machine binding as one operation. | `src/nested_artboard.cpp:156-185,228-300` | L |
| `ArtboardComponentList` occurrence advance, transform, and keep-going composition | V14, V18, V19, V39 (4) | List children advance in index order, nested state transitions are conditionally retried on settle passes, child dirt bubbles to the list, and virtualization reuses occurrence/state-machine pairs. V14/V19/V39 currently differ only in the raw `advanceAndApply` return; V18 first differs in the mounted-child transform. | `src/artboard_component_list.cpp:715-814,827-885,1660-1725`; `src/animation/state_machine_instance.cpp:2649-2707` | L |
| Scripted path-effect invalidation and feather composition | V25, V30 (2) | A scripted effect advances once per active frame, invalidates its retained effect path, then `ShapePaint::draw` selects the last effect path and rebuilds the inner-feather path only when that effect changed. | `src/scripted/scripted_path_effect.cpp:111-132,199-207`; `src/shapes/paint/shape_paint.cpp:115-152` | M |
| Mid-frame VM comparator evaluation | V11, V33 (2) | `ConditionComparisonSelf` observes `source->hasChanged()` and suppresses a change already consumed by the same layer. It must be evaluated between the same data-bind/update passes as C++. V33's property-vs-trigger comparator is explicitly lowered to this same mechanism. | `src/animation/transition_viewmodel_condition.cpp:49-60,1098-1108`; settle site `src/animation/state_machine_instance.cpp:2665-2697` | M |
| Data-bind/frame-component convergence cost | V12 (1) | C++ bounds the update/data-bind/transition settlement loop to five outer passes and resets the data context after each pass. The Rust full run spends 173.40 seconds of user CPU and still does not complete by 180 seconds. | `src/animation/state_machine_instance.cpp:2649-2707`; `src/artboard.cpp:1417-1480` | L |
| Animated compound/feather path interpolation | V16 (1) | Keyed values are applied in authored order before dependency settlement; feather/effect paths are rebuilt from the settled source path and transform. | `src/animation/linear_animation.cpp:71-84`; `src/shapes/paint/feather.cpp:36-89` | M |
| Skin/constraint dependency order | V17 (1) | A `Skin` depends on every tendon bone and its peer constraints, snapshots all bone transforms in dependency order, then deforms vertices from that buffer. | `src/bones/skin.cpp:38-64,79-94` | L |
| Keyed callback interval and listener event reporting | V28 (1) | Animation advance reports callbacks over the exact `(lastTime, newTime]` interval, including loop-wrap segments; keyed-property lookup computes delay and reports in authored order. | `src/animation/linear_animation_instance.cpp:208-217,260-307`; `src/animation/keyed_property.cpp:54-97` | M |
| Scripted `Node.asPath()` snapshot retention | V32 (1) | `asPath` exposes the current authored `Path::rawPath()` to the script draw call, and the scripted drawable advances/marks paint dirt before drawing. | `src/lua/lua_artboards.cpp:884-900`; `src/scripted/scripted_drawable.cpp:19-73,376-399` | M |
| Animated vertical-trim text layout | V37 (1) | Top/bottom trim is computed from shaped line metrics, feeds both measured bounds and render placement, and a trim-property change dirties the shape/layout path. | `src/text/text.cpp:226-290,364-386,644-647,1403-1408` | M |
| Row | First observed divergent operation at the enrolled samples | Failing subsystem / disposition |
|---|---|---|
| V11 | At t=0, `advance ... statesChanged=1` (Rust) vs `2` (C++); without side-channel, the first draw mismatch is the later layout path width `279.087891` vs `263.087891`. | VM comparator evaluation vs data-bind/settle ordering. |
| V12 | C++ completes all samples with a 2,246,690-byte stream. Rust exits 124 at 180.05s, using 173.40s user CPU and emitting no completed stream. A t=0-only run completes within 180s, isolating the failure to the added post-zero work. | Data-bind/frame-component convergence or pathological post-zero complexity. |
| V13 | At t=0.5 Rust inserts `makeEmptyRenderPath id=9` before drawing the filled compound path; C++ reuses retained path id 7 directly. | Render-path identity/cache invalidation. The annotation's “empty drawn path” wording is not what the fresh stream shows. |
| V14 | At t=1, Rust reports `settled=false`; C++ reports `settled=true`. With side-channel off, draw streams are contract-exact. | Component-list keep-going/settle return only; the recorded 724-vs-1074 clip mismatch does not reproduce. |
| V15 | Before the t=2 `advance` record, Rust starts creating gradients (first id 14); C++ has no corresponding live occurrence/resource creation. | Post-zero retained paint/occurrence realization. |
| V16 | At t=2, `drawPath` id 54 first differs by about 0.01406 in the animated compound points. | Animated compound/feather path interpolation. |
| V17 | At t=0.5, stroke path id 19 endpoints are `(6.45296,-6.45239)..(10.4287,-10.4278)` vs C++ `(15.2899,-15.2885)..(15.3052,-15.3038)`. | Skin/constraint update order. |
| V18 | At t=0.5, Rust applies translation `(1121,259)` where C++ applies identity. | Mounted component-list child/draw-order transform. |
| V19 | At t=1, Rust reports `settled=false`; C++ reports `settled=true`. With side-channel off, draw streams are contract-exact. | Component-list child-origin keep-going/settle return only. |
| V20 | At t=1, the first ellipse is radius 30 in Rust and radius 50 in C++. | Stateful nested VMI number propagation. |
| V21 | At t=1, Rust applies scale `-1.5`; C++ applies `+1.5`. | Stateful nested VMI binding/typed comparator consumption. |
| V22 | No divergence under the current contract; 147 lines align and the maximum numeric delta is `3.9e-6`. | **Artifact; see below.** |
| V23 | Before the t=0.5 `advance` record, Rust creates gradient id 24 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V24 | t=0 succeeds; adding t=0.5 fails during Rust renderer synchronization with `missing render paint for global 584`. | Retained render-paint lookup/lifetime. |
| V25 | At t=1, draw path id 2 has a radically different scripted group/effect result (C++ line has 9,515 chars vs Rust 2,461). | Scripted group/path-effect invalidation. |
| V26 | Before the t=0.5 `advance` record, Rust creates gradient id 189 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V27 | Byte-identical 417,801-byte streams from both runners. | **Artifact; see below.** |
| V28 | At t=1 C++ reports `main-event-2` with delay `0.183333337`; Rust advances to semantics with no event. The no-side draw later also differs (Rust creates a fresh empty path where C++ draws retained path id 7). | Keyed callback interval/listener event timing; downstream retained-path consequence. |
| V29 | t=0 succeeds; adding t=0.5 fails during Rust renderer synchronization with `missing render paint for global 71`. | Retained render-paint lookup/lifetime. |
| V30 | At t=0.5, draw path id 5 has different effect/feather points (first material delta: `(49.5722,6.52631)` vs Rust `(49.8774,27.9455)`). | Scripted path-effect advance and feather composition. |
| V31 | Before the t=0.5 `advance` record, Rust creates gradient id 50 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V32 | At t=0.5, Rust draws empty path id 7; C++ draws the authored seven-segment closed path. | Scripted `asPath` source/snapshot retention. |
| V33 | At t=0.5, Rust remains red `0xffff0000`; C++ applies green `0xff07fb5a`. | Trigger-valued `ConditionComparisonSelf`, sharing V11's comparator mechanism. |
| V34 | At t=0.5, path geometry is identical at the first differing line, but Rust paint is red and C++ paint is black. | Stateful nested VMI color propagation. The annotation's “first path geometry mismatch” does not reproduce. |
| V35 | Without side-channel the two 2,319-byte streams are byte-identical. With side-channel, C++ segfaults before emitting a stream on three sequential reproductions, including t=0-only. | **Artifact/harness-path failure; see below.** |
| V36 | No divergence under the current contract; 2,285 lines align and the maximum numeric delta is `3.0e-5`. | **Artifact; see below.** |
| V37 | At t=0.5, Rust text translation is y=`187.584229`; C++ is y=`182.76001` (delta 4.824219). | Animated vertical-trim shaping/layout propagation. The numeric pair in the register is stale. |
| V38 | At t=0.5, draw path id 3 first differs in the selected child artboard's later glyph/path coordinates. | Scripted VM-instance-to-artboard selection and nested state propagation. |
| V39 | **Closed:** draw and side-channel streams are exact at t=0/2/4. | Pooled row restoration now preserves concrete renderer owners. |
| V40 | Before the t=1 `advance` record, Rust creates gradient id 30 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| Row | Why it is not the recorded runtime divergence on this fresh pin |
|---|---|
| V22 | The fresh side-channel comparison passes. The streams have identical structure and only `3.9e-6` maximum numeric drift, far inside `1.3e-4`; the recorded 245x250-vs-490x362.5 layout mismatch is absent. This is consistent with a stale runner/capture used during densification, not the current fixture and pin. |
| V27 | The fresh runner outputs are byte-identical, including image buffer commands. The registered “buffer command phase” failure is absent. |
| V35 | The claimed radius 100-vs-75 draw divergence is absent: no-side streams are byte-identical. Enabling the diagnostic side-channel deterministically segfaults the C++ golden runner before t=0 output, so this row currently diagnoses a side-channel/semantics harness path, not the recorded Rust draw bug. The source `.riv` is clean at the pinned upstream commit. |
| V36 | The fresh side-channel comparison passes. The streams have identical structure and only `3.0e-5` maximum numeric drift; the recorded empty-path-vs-compound-path failure is absent. This is also consistent with stale runner/capture evidence. |
| Cluster | V-rows | Suspected pinned C++ mechanism | Upstream file:line | Fix size |
|---|---:|---|---|:---:|
| Retained renderer-resource identity across frames | V13, V15, V23, V24, V26, V29, V31, V40 (8) | A live occurrence owns one `RenderPaint`/`RenderPath`; dirt rewinds and refills it rather than creating a new backend object, and the object remains addressable through later frames. Rust instead rematerializes paths/gradients in six rows and loses the retained paint lookup in two. | `src/shapes/paint/shape_paint.cpp:50-57,173-185`; `src/shapes/paint/shape_paint_path.cpp:58-73`; `src/shapes/paint/shape_paint_mutator.cpp:7-25` | L |
| Stateful nested-artboard/VMI propagation | V20, V21, V34, V38 (4) | The active stateful VMI is bound to the mounted child before its nested state machine consumes the child's `DataContext`; artboard swaps rebuild the occurrence and its state-machine binding as one operation. | `src/nested_artboard.cpp:156-185,228-300` | L |
| `ArtboardComponentList` occurrence advance, transform, and keep-going composition | V14, V18, V19, V39 (4) | List children advance in index order, nested state transitions are conditionally retried on settle passes, child dirt bubbles to the list, and virtualization reuses occurrence/state-machine pairs. V14/V19/V39 currently differ only in the raw `advanceAndApply` return; V18 first differs in the mounted-child transform. | `src/artboard_component_list.cpp:715-814,827-885,1660-1725`; `src/animation/state_machine_instance.cpp:2649-2707` | L |
| Scripted path-effect invalidation and feather composition | V25, V30 (2) | A scripted effect advances once per active frame, invalidates its retained effect path, then `ShapePaint::draw` selects the last effect path and rebuilds the inner-feather path only when that effect changed. | `src/scripted/scripted_path_effect.cpp:111-132,199-207`; `src/shapes/paint/shape_paint.cpp:115-152` | M |
| Mid-frame VM comparator evaluation | V11, V33 (2) | `ConditionComparisonSelf` observes `source->hasChanged()` and suppresses a change already consumed by the same layer. It must be evaluated between the same data-bind/update passes as C++. V33's property-vs-trigger comparator is explicitly lowered to this same mechanism. | `src/animation/transition_viewmodel_condition.cpp:49-60,1098-1108`; settle site `src/animation/state_machine_instance.cpp:2665-2697` | M |
| Data-bind/frame-component convergence cost | V12 (1) | C++ bounds the update/data-bind/transition settlement loop to five outer passes and resets the data context after each pass. The Rust full run spends 173.40 seconds of user CPU and still does not complete by 180 seconds. | `src/animation/state_machine_instance.cpp:2649-2707`; `src/artboard.cpp:1417-1480` | L |
| Animated compound/feather path interpolation | V16 (1) | Keyed values are applied in authored order before dependency settlement; feather/effect paths are rebuilt from the settled source path and transform. | `src/animation/linear_animation.cpp:71-84`; `src/shapes/paint/feather.cpp:36-89` | M |
| Skin/constraint dependency order | V17 (1) | A `Skin` depends on every tendon bone and its peer constraints, snapshots all bone transforms in dependency order, then deforms vertices from that buffer. | `src/bones/skin.cpp:38-64,79-94` | L |
| Keyed callback interval and listener event reporting | V28 (1) | Animation advance reports callbacks over the exact `(lastTime, newTime]` interval, including loop-wrap segments; keyed-property lookup computes delay and reports in authored order. | `src/animation/linear_animation_instance.cpp:208-217,260-307`; `src/animation/keyed_property.cpp:54-97` | M |
| Scripted `Node.asPath()` snapshot retention | V32 (1) | `asPath` exposes the current authored `Path::rawPath()` to the script draw call, and the scripted drawable advances/marks paint dirt before drawing. | `src/lua/lua_artboards.cpp:884-900`; `src/scripted/scripted_drawable.cpp:19-73,376-399` | M |
| Animated vertical-trim text layout | V37 (1) | Top/bottom trim is computed from shaped line metrics, feeds both measured bounds and render placement, and a trim-property change dirties the shape/layout path. | `src/text/text.cpp:226-290,364-386,644-647,1403-1408` | M |
| Row | First observed divergent operation at the enrolled samples | Failing subsystem / disposition |
|---|---|---|
| V11 | At t=0, `advance ... statesChanged=1` (Rust) vs `2` (C++); without side-channel, the first draw mismatch is the later layout path width `279.087891` vs `263.087891`. | VM comparator evaluation vs data-bind/settle ordering. |
| V12 | C++ completes all samples with a 2,246,690-byte stream. Rust exits 124 at 180.05s, using 173.40s user CPU and emitting no completed stream. A t=0-only run completes within 180s, isolating the failure to the added post-zero work. | Data-bind/frame-component convergence or pathological post-zero complexity. |
| V13 | At t=0.5 Rust inserts `makeEmptyRenderPath id=9` before drawing the filled compound path; C++ reuses retained path id 7 directly. | Render-path identity/cache invalidation. The annotation's “empty drawn path” wording is not what the fresh stream shows. |
| V14 | At t=1, Rust reports `settled=false`; C++ reports `settled=true`. With side-channel off, draw streams are contract-exact. | Component-list keep-going/settle return only; the recorded 724-vs-1074 clip mismatch does not reproduce. |
| V15 | Before the t=2 `advance` record, Rust starts creating gradients (first id 14); C++ has no corresponding live occurrence/resource creation. | Post-zero retained paint/occurrence realization. |
| V16 | At t=2, `drawPath` id 54 first differs by about 0.01406 in the animated compound points. | Animated compound/feather path interpolation. |
| V17 | At t=0.5, stroke path id 19 endpoints are `(6.45296,-6.45239)..(10.4287,-10.4278)` vs C++ `(15.2899,-15.2885)..(15.3052,-15.3038)`. | Skin/constraint update order. |
| V18 | At t=0.5, Rust applies translation `(1121,259)` where C++ applies identity. | Mounted component-list child/draw-order transform. |
| V19 | At t=1, Rust reports `settled=false`; C++ reports `settled=true`. With side-channel off, draw streams are contract-exact. | Component-list child-origin keep-going/settle return only. |
| V20 | At t=1, the first ellipse is radius 30 in Rust and radius 50 in C++. | Stateful nested VMI number propagation. |
| V21 | At t=1, Rust applies scale `-1.5`; C++ applies `+1.5`. | Stateful nested VMI binding/typed comparator consumption. |
| V22 | No divergence under the current contract; 147 lines align and the maximum numeric delta is `3.9e-6`. | **Artifact; see below.** |
| V23 | Before the t=0.5 `advance` record, Rust creates gradient id 24 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V24 | t=0 succeeds; adding t=0.5 fails during Rust renderer synchronization with `missing render paint for global 584`. | Retained render-paint lookup/lifetime. |
| V25 | At t=1, draw path id 2 has a radically different scripted group/effect result (C++ line has 9,515 chars vs Rust 2,461). | Scripted group/path-effect invalidation. |
| V26 | Before the t=0.5 `advance` record, Rust creates gradient id 189 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V27 | Byte-identical 417,801-byte streams from both runners. | **Artifact; see below.** |
| V28 | At t=1 C++ reports `main-event-2` with delay `0.183333337`; Rust advances to semantics with no event. The no-side draw later also differs (Rust creates a fresh empty path where C++ draws retained path id 7). | Keyed callback interval/listener event timing; downstream retained-path consequence. |
| V29 | t=0 succeeds; adding t=0.5 fails during Rust renderer synchronization with `missing render paint for global 71`. | Retained render-paint lookup/lifetime. |
| V30 | At t=0.5, draw path id 5 has different effect/feather points (first material delta: `(49.5722,6.52631)` vs Rust `(49.8774,27.9455)`). | Scripted path-effect advance and feather composition. |
| V31 | Before the t=0.5 `advance` record, Rust creates gradient id 50 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V32 | At t=0.5, Rust draws empty path id 7; C++ draws the authored seven-segment closed path. | Scripted `asPath` source/snapshot retention. |
| V33 | At t=0.5, Rust remains red `0xffff0000`; C++ applies green `0xff07fb5a`. | Trigger-valued `ConditionComparisonSelf`, sharing V11's comparator mechanism. |
| V34 | At t=0.5, path geometry is identical at the first differing line, but Rust paint is red and C++ paint is black. | Stateful nested VMI color propagation. The annotation's “first path geometry mismatch” does not reproduce. |
| V35 | Without side-channel the two 2,319-byte streams are byte-identical. With side-channel, C++ segfaults before emitting a stream on three sequential reproductions, including t=0-only. | **Artifact/harness-path failure; see below.** |
| V36 | No divergence under the current contract; 2,285 lines align and the maximum numeric delta is `3.0e-5`. | **Artifact; see below.** |
| V37 | At t=0.5, Rust text translation is y=`187.584229`; C++ is y=`182.76001` (delta 4.824219). | Animated vertical-trim shaping/layout propagation. The numeric pair in the register is stale. |
| V38 | At t=0.5, draw path id 3 first differs in the selected child artboard's later glyph/path coordinates. | Scripted VM-instance-to-artboard selection and nested state propagation. |
| V39 | **Closed:** draw and side-channel streams are exact at t=0/2/4. | Pooled row restoration now preserves concrete renderer owners. |
| V40 | Before the t=1 `advance` record, Rust creates gradient id 30 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| Row | Why it is not the recorded runtime divergence on this fresh pin |
|---|---|
| V22 | The fresh side-channel comparison passes. The streams have identical structure and only `3.9e-6` maximum numeric drift, far inside `1.3e-4`; the recorded 245x250-vs-490x362.5 layout mismatch is absent. This is consistent with a stale runner/capture used during densification, not the current fixture and pin. |
| V27 | The fresh runner outputs are byte-identical, including image buffer commands. The registered “buffer command phase” failure is absent. |
| V35 | The claimed radius 100-vs-75 draw divergence is absent: no-side streams are byte-identical. Enabling the diagnostic side-channel deterministically segfaults the C++ golden runner before t=0 output, so this row currently diagnoses a side-channel/semantics harness path, not the recorded Rust draw bug. The source `.riv` is clean at the pinned upstream commit. |
| V36 | The fresh side-channel comparison passes. The streams have identical structure and only `3.0e-5` maximum numeric drift; the recorded empty-path-vs-compound-path failure is absent. This is also consistent with stale runner/capture evidence. |
| Cluster | V-rows | Suspected pinned C++ mechanism | Upstream file:line | Fix size |
|---|---:|---|---|:---:|
| Retained renderer-resource identity across frames | V13, V15, V23, V24, V26, V29, V31, V40 (8) | A live occurrence owns one `RenderPaint`/`RenderPath`; dirt rewinds and refills it rather than creating a new backend object, and the object remains addressable through later frames. Rust instead rematerializes paths/gradients in six rows and loses the retained paint lookup in two. | `src/shapes/paint/shape_paint.cpp:50-57,173-185`; `src/shapes/paint/shape_paint_path.cpp:58-73`; `src/shapes/paint/shape_paint_mutator.cpp:7-25` | L |
| Stateful nested-artboard/VMI propagation | V20, V21, V34, V38 (4) | The active stateful VMI is bound to the mounted child before its nested state machine consumes the child's `DataContext`; artboard swaps rebuild the occurrence and its state-machine binding as one operation. | `src/nested_artboard.cpp:156-185,228-300` | L |
| `ArtboardComponentList` occurrence advance, transform, and keep-going composition | V14, V18, V19, V39 (4) | List children advance in index order, nested state transitions are conditionally retried on settle passes, child dirt bubbles to the list, and virtualization reuses occurrence/state-machine pairs. V14/V19/V39 currently differ only in the raw `advanceAndApply` return; V18 first differs in the mounted-child transform. | `src/artboard_component_list.cpp:715-814,827-885,1660-1725`; `src/animation/state_machine_instance.cpp:2649-2707` | L |
| Scripted path-effect invalidation and feather composition | V25, V30 (2) | A scripted effect advances once per active frame, invalidates its retained effect path, then `ShapePaint::draw` selects the last effect path and rebuilds the inner-feather path only when that effect changed. | `src/scripted/scripted_path_effect.cpp:111-132,199-207`; `src/shapes/paint/shape_paint.cpp:115-152` | M |
| Mid-frame VM comparator evaluation | V11, V33 (2) | `ConditionComparisonSelf` observes `source->hasChanged()` and suppresses a change already consumed by the same layer. It must be evaluated between the same data-bind/update passes as C++. V33's property-vs-trigger comparator is explicitly lowered to this same mechanism. | `src/animation/transition_viewmodel_condition.cpp:49-60,1098-1108`; settle site `src/animation/state_machine_instance.cpp:2665-2697` | M |
| Data-bind/frame-component convergence cost | V12 (1) | C++ bounds the update/data-bind/transition settlement loop to five outer passes and resets the data context after each pass. The Rust full run spends 173.40 seconds of user CPU and still does not complete by 180 seconds. | `src/animation/state_machine_instance.cpp:2649-2707`; `src/artboard.cpp:1417-1480` | L |
| Animated compound/feather path interpolation | V16 (1) | Keyed values are applied in authored order before dependency settlement; feather/effect paths are rebuilt from the settled source path and transform. | `src/animation/linear_animation.cpp:71-84`; `src/shapes/paint/feather.cpp:36-89` | M |
| Skin/constraint dependency order | V17 (1) | A `Skin` depends on every tendon bone and its peer constraints, snapshots all bone transforms in dependency order, then deforms vertices from that buffer. | `src/bones/skin.cpp:38-64,79-94` | L |
| Keyed callback interval and listener event reporting | V28 (1) | Animation advance reports callbacks over the exact `(lastTime, newTime]` interval, including loop-wrap segments; keyed-property lookup computes delay and reports in authored order. | `src/animation/linear_animation_instance.cpp:208-217,260-307`; `src/animation/keyed_property.cpp:54-97` | M |
| Scripted `Node.asPath()` snapshot retention | V32 (1) | `asPath` exposes the current authored `Path::rawPath()` to the script draw call, and the scripted drawable advances/marks paint dirt before drawing. | `src/lua/lua_artboards.cpp:884-900`; `src/scripted/scripted_drawable.cpp:19-73,376-399` | M |
| Animated vertical-trim text layout | V37 (1) | Top/bottom trim is computed from shaped line metrics, feeds both measured bounds and render placement, and a trim-property change dirties the shape/layout path. | `src/text/text.cpp:226-290,364-386,644-647,1403-1408` | M |
| Row | First observed divergent operation at the enrolled samples | Failing subsystem / disposition |
|---|---|---|
| V11 | At t=0, `advance ... statesChanged=1` (Rust) vs `2` (C++); without side-channel, the first draw mismatch is the later layout path width `279.087891` vs `263.087891`. | VM comparator evaluation vs data-bind/settle ordering. |
| V12 | C++ completes all samples with a 2,246,690-byte stream. Rust exits 124 at 180.05s, using 173.40s user CPU and emitting no completed stream. A t=0-only run completes within 180s, isolating the failure to the added post-zero work. | Data-bind/frame-component convergence or pathological post-zero complexity. |
| V13 | At t=0.5 Rust inserts `makeEmptyRenderPath id=9` before drawing the filled compound path; C++ reuses retained path id 7 directly. | Render-path identity/cache invalidation. The annotation's “empty drawn path” wording is not what the fresh stream shows. |
| V14 | At t=1, Rust reports `settled=false`; C++ reports `settled=true`. With side-channel off, draw streams are contract-exact. | Component-list keep-going/settle return only; the recorded 724-vs-1074 clip mismatch does not reproduce. |
| V15 | Before the t=2 `advance` record, Rust starts creating gradients (first id 14); C++ has no corresponding live occurrence/resource creation. | Post-zero retained paint/occurrence realization. |
| V16 | At t=2, `drawPath` id 54 first differs by about 0.01406 in the animated compound points. | Animated compound/feather path interpolation. |
| V17 | At t=0.5, stroke path id 19 endpoints are `(6.45296,-6.45239)..(10.4287,-10.4278)` vs C++ `(15.2899,-15.2885)..(15.3052,-15.3038)`. | Skin/constraint update order. |
| V18 | At t=0.5, Rust applies translation `(1121,259)` where C++ applies identity. | Mounted component-list child/draw-order transform. |
| V19 | At t=1, Rust reports `settled=false`; C++ reports `settled=true`. With side-channel off, draw streams are contract-exact. | Component-list child-origin keep-going/settle return only. |
| V20 | At t=1, the first ellipse is radius 30 in Rust and radius 50 in C++. | Stateful nested VMI number propagation. |
| V21 | At t=1, Rust applies scale `-1.5`; C++ applies `+1.5`. | Stateful nested VMI binding/typed comparator consumption. |
| V22 | No divergence under the current contract; 147 lines align and the maximum numeric delta is `3.9e-6`. | **Artifact; see below.** |
| V23 | Before the t=0.5 `advance` record, Rust creates gradient id 24 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V24 | t=0 succeeds; adding t=0.5 fails during Rust renderer synchronization with `missing render paint for global 584`. | Retained render-paint lookup/lifetime. |
| V25 | At t=1, draw path id 2 has a radically different scripted group/effect result (C++ line has 9,515 chars vs Rust 2,461). | Scripted group/path-effect invalidation. |
| V26 | Before the t=0.5 `advance` record, Rust creates gradient id 189 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V27 | Byte-identical 417,801-byte streams from both runners. | **Artifact; see below.** |
| V28 | At t=1 C++ reports `main-event-2` with delay `0.183333337`; Rust advances to semantics with no event. The no-side draw later also differs (Rust creates a fresh empty path where C++ draws retained path id 7). | Keyed callback interval/listener event timing; downstream retained-path consequence. |
| V29 | t=0 succeeds; adding t=0.5 fails during Rust renderer synchronization with `missing render paint for global 71`. | Retained render-paint lookup/lifetime. |
| V30 | At t=0.5, draw path id 5 has different effect/feather points (first material delta: `(49.5722,6.52631)` vs Rust `(49.8774,27.9455)`). | Scripted path-effect advance and feather composition. |
| V31 | Before the t=0.5 `advance` record, Rust creates gradient id 50 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V32 | At t=0.5, Rust draws empty path id 7; C++ draws the authored seven-segment closed path. | Scripted `asPath` source/snapshot retention. |
| V33 | At t=0.5, Rust remains red `0xffff0000`; C++ applies green `0xff07fb5a`. | Trigger-valued `ConditionComparisonSelf`, sharing V11's comparator mechanism. |
| V34 | At t=0.5, path geometry is identical at the first differing line, but Rust paint is red and C++ paint is black. | Stateful nested VMI color propagation. The annotation's “first path geometry mismatch” does not reproduce. |
| V35 | Without side-channel the two 2,319-byte streams are byte-identical. With side-channel, C++ segfaults before emitting a stream on three sequential reproductions, including t=0-only. | **Artifact/harness-path failure; see below.** |
| V36 | No divergence under the current contract; 2,285 lines align and the maximum numeric delta is `3.0e-5`. | **Artifact; see below.** |
| V37 | At t=0.5, Rust text translation is y=`187.584229`; C++ is y=`182.76001` (delta 4.824219). | Animated vertical-trim shaping/layout propagation. The numeric pair in the register is stale. |
| V38 | At t=0.5, draw path id 3 first differs in the selected child artboard's later glyph/path coordinates. | Scripted VM-instance-to-artboard selection and nested state propagation. |
| V39 | **Closed:** draw and side-channel streams are exact at t=0/2/4. | Pooled row restoration now preserves concrete renderer owners. |
| V40 | Before the t=1 `advance` record, Rust creates gradient id 30 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| Row | Why it is not the recorded runtime divergence on this fresh pin |
|---|---|
| V22 | The fresh side-channel comparison passes. The streams have identical structure and only `3.9e-6` maximum numeric drift, far inside `1.3e-4`; the recorded 245x250-vs-490x362.5 layout mismatch is absent. This is consistent with a stale runner/capture used during densification, not the current fixture and pin. |
| V27 | The fresh runner outputs are byte-identical, including image buffer commands. The registered “buffer command phase” failure is absent. |
| V35 | The claimed radius 100-vs-75 draw divergence is absent: no-side streams are byte-identical. Enabling the diagnostic side-channel deterministically segfaults the C++ golden runner before t=0 output, so this row currently diagnoses a side-channel/semantics harness path, not the recorded Rust draw bug. The source `.riv` is clean at the pinned upstream commit. |
| V36 | The fresh side-channel comparison passes. The streams have identical structure and only `3.0e-5` maximum numeric drift; the recorded empty-path-vs-compound-path failure is absent. This is also consistent with stale runner/capture evidence. |
| Cluster | V-rows | Suspected pinned C++ mechanism | Upstream file:line | Fix size |
|---|---:|---|---|:---:|
| Retained renderer-resource identity across frames | V13, V15, V23, V24, V26, V29, V31, V40 (8) | A live occurrence owns one `RenderPaint`/`RenderPath`; dirt rewinds and refills it rather than creating a new backend object, and the object remains addressable through later frames. Rust instead rematerializes paths/gradients in six rows and loses the retained paint lookup in two. | `src/shapes/paint/shape_paint.cpp:50-57,173-185`; `src/shapes/paint/shape_paint_path.cpp:58-73`; `src/shapes/paint/shape_paint_mutator.cpp:7-25` | L |
| Stateful nested-artboard/VMI propagation | V20, V21, V34, V38 (4) | The active stateful VMI is bound to the mounted child before its nested state machine consumes the child's `DataContext`; artboard swaps rebuild the occurrence and its state-machine binding as one operation. | `src/nested_artboard.cpp:156-185,228-300` | L |
| `ArtboardComponentList` occurrence advance, transform, and keep-going composition | V14, V18, V19, V39 (4) | List children advance in index order, nested state transitions are conditionally retried on settle passes, child dirt bubbles to the list, and virtualization reuses occurrence/state-machine pairs. V14/V19/V39 currently differ only in the raw `advanceAndApply` return; V18 first differs in the mounted-child transform. | `src/artboard_component_list.cpp:715-814,827-885,1660-1725`; `src/animation/state_machine_instance.cpp:2649-2707` | L |
| Scripted path-effect invalidation and feather composition | V25, V30 (2) | A scripted effect advances once per active frame, invalidates its retained effect path, then `ShapePaint::draw` selects the last effect path and rebuilds the inner-feather path only when that effect changed. | `src/scripted/scripted_path_effect.cpp:111-132,199-207`; `src/shapes/paint/shape_paint.cpp:115-152` | M |
| Mid-frame VM comparator evaluation | V11, V33 (2) | `ConditionComparisonSelf` observes `source->hasChanged()` and suppresses a change already consumed by the same layer. It must be evaluated between the same data-bind/update passes as C++. V33's property-vs-trigger comparator is explicitly lowered to this same mechanism. | `src/animation/transition_viewmodel_condition.cpp:49-60,1098-1108`; settle site `src/animation/state_machine_instance.cpp:2665-2697` | M |
| Data-bind/frame-component convergence cost | V12 (1) | C++ bounds the update/data-bind/transition settlement loop to five outer passes and resets the data context after each pass. The Rust full run spends 173.40 seconds of user CPU and still does not complete by 180 seconds. | `src/animation/state_machine_instance.cpp:2649-2707`; `src/artboard.cpp:1417-1480` | L |
| Animated compound/feather path interpolation | V16 (1) | Keyed values are applied in authored order before dependency settlement; feather/effect paths are rebuilt from the settled source path and transform. | `src/animation/linear_animation.cpp:71-84`; `src/shapes/paint/feather.cpp:36-89` | M |
| Skin/constraint dependency order | V17 (1) | A `Skin` depends on every tendon bone and its peer constraints, snapshots all bone transforms in dependency order, then deforms vertices from that buffer. | `src/bones/skin.cpp:38-64,79-94` | L |
| Keyed callback interval and listener event reporting | V28 (1) | Animation advance reports callbacks over the exact `(lastTime, newTime]` interval, including loop-wrap segments; keyed-property lookup computes delay and reports in authored order. | `src/animation/linear_animation_instance.cpp:208-217,260-307`; `src/animation/keyed_property.cpp:54-97` | M |
| Scripted `Node.asPath()` snapshot retention | V32 (1) | `asPath` exposes the current authored `Path::rawPath()` to the script draw call, and the scripted drawable advances/marks paint dirt before drawing. | `src/lua/lua_artboards.cpp:884-900`; `src/scripted/scripted_drawable.cpp:19-73,376-399` | M |
| Animated vertical-trim text layout | V37 (1) | Top/bottom trim is computed from shaped line metrics, feeds both measured bounds and render placement, and a trim-property change dirties the shape/layout path. | `src/text/text.cpp:226-290,364-386,644-647,1403-1408` | M |
| Row | First observed divergent operation at the enrolled samples | Failing subsystem / disposition |
|---|---|---|
| V11 | At t=0, `advance ... statesChanged=1` (Rust) vs `2` (C++); without side-channel, the first draw mismatch is the later layout path width `279.087891` vs `263.087891`. | VM comparator evaluation vs data-bind/settle ordering. |
| V12 | C++ completes all samples with a 2,246,690-byte stream. Rust exits 124 at 180.05s, using 173.40s user CPU and emitting no completed stream. A t=0-only run completes within 180s, isolating the failure to the added post-zero work. | Data-bind/frame-component convergence or pathological post-zero complexity. |
| V13 | At t=0.5 Rust inserts `makeEmptyRenderPath id=9` before drawing the filled compound path; C++ reuses retained path id 7 directly. | Render-path identity/cache invalidation. The annotation's “empty drawn path” wording is not what the fresh stream shows. |
| V14 | At t=1, Rust reports `settled=false`; C++ reports `settled=true`. With side-channel off, draw streams are contract-exact. | Component-list keep-going/settle return only; the recorded 724-vs-1074 clip mismatch does not reproduce. |
| V15 | Before the t=2 `advance` record, Rust starts creating gradients (first id 14); C++ has no corresponding live occurrence/resource creation. | Post-zero retained paint/occurrence realization. |
| V16 | At t=2, `drawPath` id 54 first differs by about 0.01406 in the animated compound points. | Animated compound/feather path interpolation. |
| V17 | At t=0.5, stroke path id 19 endpoints are `(6.45296,-6.45239)..(10.4287,-10.4278)` vs C++ `(15.2899,-15.2885)..(15.3052,-15.3038)`. | Skin/constraint update order. |
| V18 | At t=0.5, Rust applies translation `(1121,259)` where C++ applies identity. | Mounted component-list child/draw-order transform. |
| V19 | At t=1, Rust reports `settled=false`; C++ reports `settled=true`. With side-channel off, draw streams are contract-exact. | Component-list child-origin keep-going/settle return only. |
| V20 | At t=1, the first ellipse is radius 30 in Rust and radius 50 in C++. | Stateful nested VMI number propagation. |
| V21 | At t=1, Rust applies scale `-1.5`; C++ applies `+1.5`. | Stateful nested VMI binding/typed comparator consumption. |
| V22 | No divergence under the current contract; 147 lines align and the maximum numeric delta is `3.9e-6`. | **Artifact; see below.** |
| V23 | Before the t=0.5 `advance` record, Rust creates gradient id 24 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V24 | t=0 succeeds; adding t=0.5 fails during Rust renderer synchronization with `missing render paint for global 584`. | Retained render-paint lookup/lifetime. |
| V25 | At t=1, draw path id 2 has a radically different scripted group/effect result (C++ line has 9,515 chars vs Rust 2,461). | Scripted group/path-effect invalidation. |
| V26 | Before the t=0.5 `advance` record, Rust creates gradient id 189 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V27 | Byte-identical 417,801-byte streams from both runners. | **Artifact; see below.** |
| V28 | At t=1 C++ reports `main-event-2` with delay `0.183333337`; Rust advances to semantics with no event. The no-side draw later also differs (Rust creates a fresh empty path where C++ draws retained path id 7). | Keyed callback interval/listener event timing; downstream retained-path consequence. |
| V29 | t=0 succeeds; adding t=0.5 fails during Rust renderer synchronization with `missing render paint for global 71`. | Retained render-paint lookup/lifetime. |
| V30 | At t=0.5, draw path id 5 has different effect/feather points (first material delta: `(49.5722,6.52631)` vs Rust `(49.8774,27.9455)`). | Scripted path-effect advance and feather composition. |
| V31 | Before the t=0.5 `advance` record, Rust creates gradient id 50 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| V32 | At t=0.5, Rust draws empty path id 7; C++ draws the authored seven-segment closed path. | Scripted `asPath` source/snapshot retention. |
| V33 | At t=0.5, Rust remains red `0xffff0000`; C++ applies green `0xff07fb5a`. | Trigger-valued `ConditionComparisonSelf`, sharing V11's comparator mechanism. |
| V34 | At t=0.5, path geometry is identical at the first differing line, but Rust paint is red and C++ paint is black. | Stateful nested VMI color propagation. The annotation's “first path geometry mismatch” does not reproduce. |
| V35 | Without side-channel the two 2,319-byte streams are byte-identical. With side-channel, C++ segfaults before emitting a stream on three sequential reproductions, including t=0-only. | **Artifact/harness-path failure; see below.** |
| V36 | No divergence under the current contract; 2,285 lines align and the maximum numeric delta is `3.0e-5`. | **Artifact; see below.** |
| V37 | At t=0.5, Rust text translation is y=`187.584229`; C++ is y=`182.76001` (delta 4.824219). | Animated vertical-trim shaping/layout propagation. The numeric pair in the register is stale. |
| V38 | At t=0.5, draw path id 3 first differs in the selected child artboard's later glyph/path coordinates. | Scripted VM-instance-to-artboard selection and nested state propagation. |
| V39 | **Closed:** draw and side-channel streams are exact at t=0/2/4. | Pooled row restoration now preserves concrete renderer owners. |
| V40 | Before the t=1 `advance` record, Rust creates gradient id 30 while C++ creates no corresponding resource. | Post-zero retained paint/occurrence realization. |
| Row | Why it is not the recorded runtime divergence on this fresh pin |
|---|---|
| V22 | The fresh side-channel comparison passes. The streams have identical structure and only `3.9e-6` maximum numeric drift, far inside `1.3e-4`; the recorded 245x250-vs-490x362.5 layout mismatch is absent. This is consistent with a stale runner/capture used during densification, not the current fixture and pin. |
| V27 | The fresh runner outputs are byte-identical, including image buffer commands. The registered “buffer command phase” failure is absent. |
| V35 | The claimed radius 100-vs-75 draw divergence is absent: no-side streams are byte-identical. Enabling the diagnostic side-channel deterministically segfaults the C++ golden runner before t=0 output, so this row currently diagnoses a side-channel/semantics harness path, not the recorded Rust draw bug. The source `.riv` is clean at the pinned upstream commit. |
| V36 | The fresh side-channel comparison passes. The streams have identical structure and only `3.0e-5` maximum numeric drift; the recorded empty-path-vs-compound-path failure is absent. This is also consistent with stale runner/capture evidence. |
